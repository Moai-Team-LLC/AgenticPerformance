############### FEASIBILITY CONTRADICTIONS (30) ###############

- [FR-CONTRACT-1] The engine's real, shipped telemetry standard is OpenInference (Phoenix/Langfuse/LangSmith), NOT OTel GenAI semconv. FR-CONTRACT-1 mandates GenAI semconv as the base; adopting it is net-new AND collides with the existing OpenInference why-trace already flowing. 'own format not introduced' is technically true but the PRD ignores that a *different* third-party convention is already the incumbent.
  evidence: packages/shared/src/lib/observability/trace.ts:9-41 (uses OpenInference conventions: openinference.span.kind, llm.model_name, input.value/output.value); grep for 'gen_ai.' across packages/apps/scripts returns zero hits
- [FR-CONTRACT-2] PRD prescribes bare canonical span *names* invoke_agent/chat/execute_tool. Per the actual semconv, 'chat' is the operation.name, not the span name; the span name is operation+model. Prescribing 'chat' as a span name is a semconv error and the current code names spans by pipeline phase, not by GenAI operation.
  evidence: OTel GenAI semconv (context7 /open-telemetry/semantic-conventions): chat span name SHOULD be '{gen_ai.operation.name} {gen_ai.request.model}', tool span 'execute_tool {gen_ai.tool.name}'. trace.ts:20-25 defines only CHAIN/RETRIEVER/LLM span kinds, span names are literal ('knowledge.ask','retrieve','synthesize')
- [FR-CONTRACT-4] The opt-in env var is honored by *instrumentation libraries*, not the SDK or semconv package. AgenticMind uses none, so the flag is a no-op here. FR-CONTRACT-4 assumes an auto-instrumentation stack that doesn't exist in the repo.
  evidence: No file under node_modules/@opentelemetry reads OTEL_SEMCONV_STABILITY_OPT_IN or gen_ai_latest_experimental (grep -rl = 0). The repo hand-rolls spans on @opentelemetry/api (trace.ts); there are NO OTel auto-instrumentation libraries installed (only api/sdk-trace-node/exporter/resources/semantic-conventions in apps/{server,worker}/package.json)
- [C2] C2 claims 'opt-in for agent/tool spans, version pinned' as if the pinned version supplies them. It does not — the pinned 1.41.x line predates/omits GenAI, which has been spun out into semantic-conventions-genai. The constraint is self-defeating as written.
  evidence: node_modules/@opentelemetry/semantic-conventions is pinned to 1.41.1 (apps/server & apps/worker package.json '^1.41.1'); its incubating build has 0 gen_ai constants. GenAI attrs live in a *separate* repo now (context7: 'Moved to the OpenTelemetry GenAI semantic conventions repository')
- [FR-SDK-4 (tail-sampling: ~5% prod, 100% on errors and slow)] Tail sampling (keep-on-error/keep-on-slow) is a decision made AFTER a trace completes across all spans — an in-process SDK cannot do it without buffering whole traces; it requires the OTel Collector tailsamplingprocessor. This forces the Collector into the required path, contradicting 'thin SDK'. Head sampling is doable in-process, but head sampling CANNOT satisfy '100% on errors' (error is unknown at root-span start).
  evidence: apps/server/src/tracing.ts wires a NodeTracerProvider with BatchSpanProcessor and NO Sampler; exporter-trace-otlp-http ^0.218.0, sdk-trace-node ^2.7.1. No Sampler/ParentBased/TailSampling anywhere (grep = 0). Sampling is currently all-or-nothing via presence of OTEL_EXPORTER_OTLP_ENDPOINT.
- [FR-SDK-5 (PII redaction on emit; collector redaction processor OR Presidio sidecar; before export)] 'Redact on emit before export' via a collector processor happens OUTSIDE the process the SDK runs in, so it cannot hold as an SDK guarantee; and it does not cover the ClickHouse path if traces are written there directly. Choosing 'collector processor OR Presidio sidecar' is left unresolved and the two have different trust boundaries.
  evidence: grep Presidio/presidio = 0. The only PII code is packages/shared/src/lib/knowledge/guard.ts: regex-first INPUT-side redaction (email/phone/card/SSN/IPv4) applied to mem_write beliefs — NOT a span-export redaction processor. tracing.ts has no span processor between generation and BatchSpanProcessor.
- [C3 / FR-INGEST-3 (ClickHouse mandate)] Mandating ClickHouse directly fights the engine's flagship stated non-goal. This is a fork of the architecture, not a module of it.
  evidence: ROADMAP.md:36 Non-goal: 'Multi-datastore architecture. It stays Postgres-only on purpose.'; README.md:46 'One datastore — Postgres + pgvector ... No Redis, no Neo4j, no vector-DB sprawl'; CONTRIBUTING.md:23 lists ClickHouse as a hard-dependency to keep behind an interface; grep found zero ClickHouse code anywhere in src.
- [FR-INGEST-3 (trace stream in ClickHouse)] The append-heavy analytics need is real, but the engine already carries TimescaleDB for exactly this. Introducing ClickHouse adds a second datastore the project explicitly refuses.
  evidence: Same as C3. Plus docker-compose.yml:5 already ships TimescaleDB (hypertables/columnar-compression/retention policies) which covers append-heavy time-series on the existing Postgres; grep for create_hypertable/add_retention_policy/compress = none used yet.
- [FR-INGEST-5 (semantic search over fail-traces via pgvector)] Internally contradictory with FR-INGEST-3: you cannot run a pgvector diskann index over rows that live in ClickHouse. Requires copying trace/embedding data back into Postgres — a cross-store ETL the PRD never specifies.
  evidence: vector+diskann pattern exists (chunks.ts:50, ask-clusters.ts:50 'diskann' vector_cosine_ops) — technically reusable. BUT FR-INGEST-3 puts traces in ClickHouse while FR-INGEST-5 requires pgvector over those same traces.
- [FR-REG-3 (every trace/eval_run/improvement FK -> (agent_id, agent_version))] The mandated referential integrity is unenforceable across the mandated store split. Either both live in Postgres (contradicts C3) or the FK is a soft convention (contradicts FR-REG-3 attribution requirement).
  evidence: FR-INGEST-3 places traces in ClickHouse; FR-REG-2 places agent_version in Postgres. A Postgres FK cannot reference/enforce against a ClickHouse row (and ClickHouse has no enforced FKs at all).
- [§8 scorecard] A materialized scorecard TABLE is net-new and fine in Postgres; but FR-SCORE's control-plane framing collides with the headless non-goal. Storable, but the consuming UI is out of the engine's scope.
  evidence: ROADMAP.md:35 Non-goal: 'A frontend / admin UI. AgenticMind is a headless substrate; agents are the only consumers.' A materialized scorecard/control-plane view implies a read/query surface for humans.
- [C4 (tenant isolation cited as v1.4.0)] Tenant isolation EXISTS and is reusable, but the cited version is wrong (no v1.4.0). More importantly RLS is Postgres-GUC-based and cannot extend to ClickHouse (see finding).
  evidence: Repo is package.json version 0.8.0; tenant isolation is real (drizzle/0003_tenant_isolation.sql: ENABLE/FORCE ROW LEVEL SECURITY + CREATE POLICY per table; _tenant.ts GUC app.current_tenant).
- [FR-EVAL-1 (golden set per-agent; no global suite)] The existing suite is the mirror image of what FR-EVAL-1 mandates: exactly ONE global cases.json for ONE agent. There is no agent registry, no per-agent partition, no eval_case table. Building 'golden-set-per-agent' is net-new keyed on a registry that also does not exist. Reusable pattern (failureMode buckets, self-validating generator) but not the per-agent structure.
  evidence: /Users/duchenchuk/Desktop/AgenticMind/eval/cases.json (single global file, 0 occurrences of agent_id/agentId/agent_version); scripts/eval.ts loads that one file and hardwires the single knowledge `ask` engine (lib/knowledge/ask.ts) as the only system under test
- [FR-EVAL-2c (rotating HUMAN sample as ground truth)] The repo made a deliberate design decision that the top of the pyramid is a stronger-MODEL oracle, not humans, and its calibration ground truth is synthetic-by-construction, not human. FR-EVAL-2c / FR-JUDGE-2's 100-human-labels premise fights the existing implementation choice and has no human-labeling surface to reuse.
  evidence: /Users/duchenchuk/Desktop/AgenticMind/eval/README.md:39-41 — Level 3 is explicitly 'an agent/stronger-model pass, not a human one'; eval/judge-labels.json = 129 labels of which only 19 are hand-authored and 110 are synthetically constructed by scripts/build-eval-cases.ts:325-340
- [FR-EVAL-3 (golden set grows weekly from mined prod failures → label → add case)] The only prod audit trail deliberately discards the content you need to synthesize an eval case (privacy comment: 'only the sha256 of the normalised question is stored, never the question text'). The 'grow from prod' loop is today a manual edit of candidate arrays in a script. Feeding FR-EVAL-3 requires net-new content capture (which fights NFR-PRIV-1/PII redaction) plus a labeling pipeline that does not exist.
  evidence: packages/shared/src/database/schema/knowledge/ask-telemetry.ts:26-33 stores only questionHash (sha256), servedBy, latency/cost, citationCount — NO question text, NO answer, NO trace; scripts/build-eval-cases.ts grows cases from a hand-curated FACTS[] array verified against a vendored corpus, not from prod
- [FR-EVAL-4 (CI-gate blocks deploy on regression relative to CURRENT PROD version, per agent_version change)] Two gaps: (1) the gate is against a static threshold, not 'the current prod version', and there is no agent_version registry or eval_run history to diff against; (2) the real LLM-driven eval is not actually wired into CI (needs DATABASE_URL + CHAT_API_KEY that the pipeline doesn't provide). What runs in CI is deterministic harness logic on stub data.
  evidence: packages/shared/src/lib/eval/harness.ts:194-198 isRegression compares report.passRate against a FIXED baselinePassRate constant (scripts/eval.ts:47 BASELINE_PASS_RATE default 0.8), not against a stored prior-version score; .github/workflows/ci.yml:46-48 the integration eval is a COMMENTED-OUT note, not an executed step — only the pure-logic harness unit test runs in CI
- [FR-EVAL-5 (100% prod traffic yields traces reusable for eval)] Prod telemetry today is aggregate metrics keyed on a question hash. It is not 'traces pripodnye for eval-set replenishment' — the payload needed to replay/label a case is intentionally absent. Net-new capture required, in direct tension with the privacy posture.
  evidence: packages/shared/src/database/schema/knowledge/ask-telemetry.ts — content-free (hash-only) telemetry; no trace/span table anywhere in packages/shared/src/database/schema/
- [R1 (ban on a single global eval suite)] The anti-pattern R1 forbids is precisely the shape of the current, working implementation. Not a defect in the repo (it has one agent) but it means R1 describes a to-be-built structure with zero existing scaffolding.
  evidence: eval/cases.json is a single global suite; scripts/{eval,tune}.ts load it globally for the one engine
- [FR-JUDGE-4 (reuse human annotations for BOTH calibration AND error taxonomy)] Sharing one annotation pool for calibration and for the taxonomy the judge is later scored against is label leakage — the repo's own README calls out exactly this circularity for L3. Marked contradiction against an in-repo methodological rule.
  evidence: eval/judge-labels.json (calibration set) vs the clustering taxonomy path packages/shared/src/lib/knowledge/clustering.ts + ask-clusters. eval/README.md line 40-41 explicitly warns: 'keep the oracle distinct from the judge under test to avoid circularity.'
- [FR-IMPROVE-3 / R2 (independent calibrated judge confirms improvement)] The PRD's 'independent' judge shares prompt lineage and label heritage with the judge/evals it validates. Without a distinct oracle, L3 auto-merge is self-referential — flagged as contradiction with the standard the repo vendors.
  evidence: feedback-promoter.ts uses the SAME JUDGE_SYSTEM/defaultJudge for gating; eval/README.md L3 requires 'keep the oracle distinct from the judge under test'. There is one judge prompt in the whole repo (grep: only feedback-judge.ts::JUDGE_SYSTEM).
- [FR-FAIL-4 (cost/latency telemetry per agent / per version / per cluster)] Per-cluster cost/latency rollups are exactly the append-heavy analytics the PRD routes to ClickHouse, which is a stated non-goal of the host engine. Per-version cost is impossible without the (also missing) agent_version registry. Marked contradiction because the mandated storage path fights an explicit AgenticMind non-goal.
  evidence: ask-telemetry.ts captures retrievalMs/generationMs/model but is keyed by servedBy path ('cache'|'card_synth'|'synth') and questionHash — never joined to a cluster, and there is no agent or agent_version table to join on. FR-INGEST-3/C3 mandate ClickHouse for this analytics; ROADMAP.md:36 'Multi-datastore architecture. It stays Postgres-only on purpose.' and CONTRIBUTING.md:22-23 forbid a hard ClickHouse dependency.
- [FR-IMPROVE-3 (L3 judge-gated auto: propose->eval->judge->auto-merge canary->A/B->promote/rollback)] L3 as specified is unbuildable within MVP by the PRD's own scope. Its two hard dependencies (A/B infra, mature golden set per A2) are both deferred/absent.
  evidence: §2 states 'Также вне MVP: A/B-инфраструктура промоушена (переносится в фазу 5)' and §12 Phase 5 bundles L3+A/B; yet FR-IMPROVE-3 defines L3 AS canary+A/B. No canary/rollout/A-B code exists (grep across packages/apps/scripts = 0 hits). selectBestParams (retrieval-params.ts) gates auto-tuning but only over a fixed retrieval-knob grid via env var, no canary/versioning.
- [A2 (L3 assumes mature golden set + calibrated judge per agent)] A2 quietly assumes away the very deliverables (per-agent golden set + per-agent calibrated judge) that are net-new and gated behind later phases; makes L3 non-startable in MVP.
  evidence: Golden sets/judges today are single-corpus (eval/cases.json, eval/judge-labels.json), FR-EVAL-1 mandates per-agent golden sets (net-new), and A/B is deferred (§2). A2's precondition cannot hold in MVP.
- [FR-INTEG-4 (SaaS and self-hosted/VPC with identical contract, incl. ClickHouse)] 'Identical contract' requires every VPC customer to run+operate ClickHouse (+ an OTLP Collector) alongside Postgres. That directly contradicts the stated Postgres-only non-goal and the actual one-container deploy. It is the single largest operational-burden and packaging risk in the whole PRD.
  evidence: deploy/docker-compose.yml (single Postgres/timescaledb-ha container, no ClickHouse, no OTel Collector); deploy/gen-secrets.sh (VPC operator provisions Postgres only); ROADMAP.md Non-goals: 'Multi-datastore architecture. It stays Postgres-only on purpose.'
- [FR-SCORE-1 (per-agent scorecard view: version, score curve, clusters, budgets, escalation %, tool-call success %, pending approvals)] A per-agent review view with 'pending approvals' is an interactive operator UI. AgenticMind is deliberately headless with agents as the only consumers. The PRD never says WHERE this UI lives (new app? separate product?). It contradicts a first-class non-goal and has no home in the current apps/{server,worker} topology.
  evidence: ROADMAP.md Non-goals: 'A frontend / admin UI. AgenticMind is a headless substrate; agents are the only consumers.'; README.md lines 130,182-183 'There is no frontend', 'ships no admin UI'
- [FR-SCORE-3 (fleet view across all products ranked by agent health)] Fleet ranking is a second, heavier control-plane UI spanning all products/tenants. No product_id exists in the data model, and cross-tenant aggregation fights the FORCE-RLS isolation model (a fleet view must deliberately bypass RLS — a privileged read path that does not exist).
  evidence: Same headless/no-UI non-goal (ROADMAP.md); no cross-product 'product_id' dimension in schema (only tenant_id in _tenant.ts)
- [NFR-SCALE-1 (trace store scales to high-frequency products via ClickHouse; configurable prod sampling)] Same ClickHouse contradiction as FR-INGEST-3/FR-INTEG-4. Sampling policy also has no implementation seam today (no SDK, no tail-sampling collector config in deploy).
  evidence: No ClickHouse anywhere (only CONTRIBUTING.md:23 mentions it as a thing to hide behind an interface); ROADMAP Postgres-only non-goal
- [NFR-TENANT-1 (tenant data isolated; cross-tenant leak impossible at storage and query level)] The isolation guarantee is a Postgres RLS property. Moving the high-volume trace store to ClickHouse (FR-INGEST-3) puts the LARGEST, most PII-laden dataset OUTSIDE the only isolation mechanism the product has. ClickHouse tenant isolation would be app-enforced WHERE-clauses (exactly what RLS was designed to make unbypassable). This is a direct security regression versus the current guarantee.
  evidence: drizzle/0003_tenant_isolation.sql (FORCE ROW LEVEL SECURITY + CREATE POLICY per Postgres table); client.ts withTenant sets app.current_tenant GUC. ClickHouse has NO analogous forced RLS.
- [NFR-PORT-1 (vendor-neutral; readable by any OTel backend; no format lock-in)] Internal tension inside the PRD itself: scorecards/registry/clustering all key off moai.* attributes. A generic OTel backend receives spans but cannot interpret moai.* — so 'no lock-in' is true only for raw span transport, false for any useful APL semantics. Also moai.* appears nowhere in the repo today (grep = 0).
  evidence: apps/server/src/tracing.ts OTLP export is genuinely neutral, BUT FR-CONTRACT-5 mandates proprietary moai.* attributes (agent_id, agent_version, outcome, decision_reason) that carry the semantics APL depends on
- [G5 (one layer runs in SaaS AND self-hosted/VPC over multi-tenant isolation)] G5 is the headline goal and it collides with two realities at once: the Postgres-only architecture and the fact that the isolation mechanism (RLS) cannot follow the data into ClickHouse. 'Same contract' across SaaS/VPC while also mandating ClickHouse is not achievable without either dropping ClickHouse or building a new isolation layer.
  evidence: ROADMAP Postgres-only non-goal + single-container deploy (deploy/docker-compose.yml) vs mandated ClickHouse; RLS isolation does not cover ClickHouse