{
  "name": "agent-performance-layer",
  "version": "0.4.0",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "tsc": "tsc --noEmit -p packages/core/tsconfig.json && tsc --noEmit -p apps/ingest/tsconfig.json && tsc --noEmit -p apps/worker/tsconfig.json",
    "test": "vitest run",
    "ingest": "dotenvx run -f .env.local -- bun apps/ingest/src/index.ts",
    "worker": "dotenvx run -f .env.local -- bun apps/worker/src/index.ts",
    "test:watch": "vitest",
    "db:generate": "dotenvx run -f .env.local -- drizzle-kit generate",
    "db:migrate-local": "dotenvx run -f .env.local -- drizzle-kit migrate",
    "prepare": "husky"
  },
  "dependencies": {
    "drizzle-kit": "0.31.10",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "@dotenvx/dotenvx": "^2.8.0",
    "@types/node": "^26.1.1",
    "husky": "^9.1.7",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "packageManager": "bun@1.3.11"
}
