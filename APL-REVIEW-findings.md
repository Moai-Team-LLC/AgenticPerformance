COUNTS: {'dimensions': 8, 'findingsTotal': 53, 'verified': 26, 'survivors': 22, 'refuted': 4, 'contradictions': 30}

LEGEND verdict: CONFIRMED=survived adversarial verify; PLAUSIBLE=real but arguable; unverified=beyond top-26 cap, not independently checked

====================================================================================================
[CRITICAL] CONFIRMED  integ-nfr-score#2  (integ-nfr-score)
FR: FR-INGEST-3, FR-INTEG-4, NFR-SCALE-1, NFR-TENANT-1, G5
TITLE: Mandating ClickHouse breaks the Postgres-only architecture AND puts the trace store outside the only tenant-isolation mechanism
CLAIM: FR-INGEST-3/FR-INTEG-4/NFR-SCALE-1 mandate ClickHouse for the trace stream in BOTH SaaS and VPC with an 'identical contract', but (1) ROADMAP.md declares 'Multi-datastore architecture. It stays Postgres-only on purpose' a non-goal, (2) the actual deploy is a single Postgres container (deploy/docker-compose.yml, deploy/gen-secrets.sh), and (3) tenant isolation is implemented exclusively as Postgres FORCE ROW LEVEL SECURITY keyed on the app.current_tenant GUC (drizzle/0003_tenant_isolation.sql, client.ts withTenant) — a mechanism ClickHouse does not have.
WHY: Two failures compound. Operationally: every VPC/enterprise customer must now provision and operate a ClickHouse cluster (+ an OTLP Collector, also absent from deploy) beside Postgres — the exact operational burden the Postgres-only stance was chosen to avoid, and a direct hit to G5's 'same contract'. Security: traces are the highest-volume, most PII-laden dataset, and moving them to ClickHouse moves them OUT of RLS. Cross-tenant isolation in ClickHouse would degrade from unbypassable DB-enforced RLS to hand-written WHERE tenant_id = ? clauses in application/query code — precisely the class of bug RLS exists to prevent. One forgotten predicate in an analytics query leaks Tenant A's agent traces to Tenant B, and NFR-TENANT-1 ('cross-tenant leak impossible at storage and query level') is violated for the biggest table.
FIX: Pick one: (a) drop ClickHouse for MVP and keep traces in Postgres with sampling + retention + partitioning (honors the non-goal, keeps RLS, matches the real deploy), promoting ClickHouse to a later 'scale' tier behind an interface as CONTRIBUTING.md already prescribes; or (b) if ClickHouse is truly required, treat it as net-new architecture: design and BUILD a ClickHouse tenant-isolation layer (per-tenant row policies or a mandatory query-builder that injects tenant_id, with tests proving A can't read B), add ClickHouse+Collector to the deploy stack, and rewrite G5 to acknowledge the heavier VPC footprint. Do not claim 'identical contract' until the isolation gap is closed.
VERIFY: All three repo facts verified verbatim. ROADMAP.md:36 declares "Multi-datastore architecture. It stays Postgres-only on purpose" a non-goal, while PRD C3 (line 215) hard-fixes ClickHouse + OTel Collector into the stack and C1 (line 213) scopes APL as a module of AgenticMind — an unacknowledged internal contradiction the PRD never reconciles (no risk R#, no open question Q# raises it). deploy/docker-compose.yml is a single Postgres (timescaledb-ha:pg17) + migrate/server/worker; ClickHouse and Collector are entirely absent, so VPC customers must provision net-new infra, directly undercutting FR-INTEG-4's "identical contract" / G5. Security prong holds: tenant isolation is exclusively Postgres FORCE ROW LEVEL SECURITY keyed on app.current_tenant (drizzle/0003_tenant_isolation.sql, client.ts withTenant via set_config transaction-local) — confirmed as the ONLY mechanism. FR-INTEG-1 (line 161) and C4 (line 216) promise APL "reuses AgenticMind tenant-isolation," yet FR-INGEST-3/NFR-SCALE-1 move the highest-volume, most PII-laden dataset (traces carry moai.* tenant attrs + FR-CONTRACT-7 message/instruction content) into ClickHouse, where that RLS+GUC mechanism does not exist. NFR-TENANT-1 promises leak "impossible at storage and query level" (MUST) but the PRD specifies nothing for how ClickHouse enforces it — Q2 only covers retention-by-tenant, not isolation. A MUST-level absolute promised via a mechanism that provably cannot cover the biggest table, with no alternative design, warrants critical.
STEELMAN: A PRD is allowed to supersede a parent product's prior non-goal: introducing ClickHouse for append-heavy trace analytics is a defensible, even standard, scaling choice, and Postgres-only was AgenticMind's stance for its knowledge substrate, not necessarily an eternal constraint on every future module. ClickHouse is not isolation-incapable — it supports CREATE ROW POLICY and settings profiles — so the gap is "the reused RLS mechanism doesn't port and a replacement is unspecified," not "isolation is impossible," and a competent team could close it with a per-tenant row policy or mandatory tenant-injecting query layer. The finding slightly overstates by asserting ClickHouse "does not have" tenant isolation. But even granting all this, the PRD's defect stands: it makes the contradiction and the isolation substitution silently, promises reuse of a mechanism that isn't there, and asserts an absolute MUST (leak impossible) with zero design to back it on the largest, most sensitive dataset — precisely what PRD review exists to catch.

====================================================================================================
[HIGH] CONFIRMED  ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3)#1  (ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3))
FR: C3, FR-INGEST-3, NFR-SCALE-1, C1
TITLE: ClickHouse mandate makes APL a fork of AgenticMind's architecture, not a module of it — and TimescaleDB (already shipped) covers the stated need
CLAIM: C3/FR-INGEST-3 hard-mandate ClickHouse for the trace stream, but AgenticMind's flagship non-goal (ROADMAP.md:36) is 'Multi-datastore architecture. It stays Postgres-only on purpose', and README.md:46 sells 'One datastore ... no vector-DB sprawl'. There is zero ClickHouse in the codebase; the only mention (CONTRIBUTING.md:23) lists it as a dependency to KEEP OUT behind an interface. Meanwhile docker-compose.yml:5 already ships timescale/timescaledb-ha:pg17 whose hypertables/columnar compression/retention policies are purpose-built for append-heavy time-series analytics.
WHY: Positioning APL as 'a module of AgenticMind' (C1) while mandating a second datastore the engine explicitly refuses means it can never merge upstream and forfeits the engine's core selling point. Failure scenario: a self-hosted/VPC enterprise tenant (G5, FR-INGEST) is promised 'self-hostable on Postgres alone' by the engine, then APL forces them to also operate, secure, back up, and tenant-isolate a ClickHouse cluster — doubling ops surface and breaking the one-datastore promise that justified choosing AgenticMind.
FIX: Drop the ClickHouse mandate. Store traces/spans as a TimescaleDB hypertable in the existing Postgres (create_hypertable + native compression + add_retention_policy for FR-INGEST-4). This keeps APL a true module, satisfies NFR-SCALE-1 for realistic write volume, and lets FR-INGEST-5 pgvector search run over the same rows. If a genuine ClickHouse-scale product later appears, gate it behind the CONTRIBUTING.md interface pattern as an optional adapter — not a MUST.
VERIFY: All cited facts verified. ROADMAP.md:36 says verbatim "Multi-datastore architecture. It stays Postgres-only on purpose"; README.md:46 sells "One datastore ... no vector-DB sprawl"; CONTRIBUTING.md ground-rule #1 names ClickHouse specifically as a dep that "belong[s] behind an interface... not in the default path"; grep confirms ZERO ClickHouse in code (only that CONTRIBUTING mention). Against this, the PRD hard-mandates ClickHouse as MUST in the default path: FR-INGEST-3 (MUST, §6.106), NFR-SCALE-1 (§7.171 names ClickHouse as the scaling mechanism), C3 (§10.215 "стек фиксирован ... ClickHouse"), and the header stack (§10). Simultaneously C1/§1.19 positions APL as "a module of AgenticMind" and G5/FR-INTEG-4 make self-hosted/VPC a MUST with "identical contract." The PRD never reconciles this: it does not acknowledge the Postgres-only non-goal, does not use the CONTRIBUTING interface/adapter pattern, and does not address the ops-doubling for VPC tenants (Q2 covers only retention policy; Q4 only open-core split). So the doc does not already handle it — a module MUST-mandating a datastore its host engine names as a flagship non-goal is a genuine, unaddressed contradiction that undercuts the one-datastore selling point for the very enterprise tenants G5 targets. One correction to the finding: the shipped timescale/timescaledb-ha image is used ONLY for bundled pgvector/pgvectorscale (diskann) — zero hypertables/compression/retention exist in the schema — so "TimescaleDB already covers the need" is a viable path, not a shipped capability; this weakens the "already covered" phrasing but not the core contradiction.
STEELMAN: APL's OTel span stream is a genuinely different workload from AgenticMind's RAG substrate. The engine's "Postgres-only" non-goal is arguably scoped to its retrieval core (vectors/FTS/graph/queue), and CONTRIBUTING.md:23 does not ban ClickHouse — it explicitly anticipates ClickHouse-class deps living behind an interface, an escape hatch that a trace-analytics module can legitimately use. Append-heavy, high-cardinality span analytics at high-frequency-product scale (NFR-SCALE-1) is the canonical ClickHouse use case (Langfuse, SigNoz), so the engineering instinct is defensible. The finding's own fix also overreaches: the repo ships none of Timescale's time-series features today, so Postgres/Timescale coverage is aspirational, not proven at APL's stated scale. The real defect is narrow — the MUST/"fixed stack" framing in the default path, not the existence of ClickHouse as an option — which is a spec-wording softening (MUST→optional adapter behind the CONTRIBUTING interface), not a redesign.

====================================================================================================
[HIGH] CONFIRMED  ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3)#3  (ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3))
FR: NFR-TENANT-1, FR-INTEG-1, C4, FR-INGEST-3
TITLE: Tenant isolation is Postgres-RLS/GUC-based and does NOT extend to ClickHouse — NFR-TENANT-1 breaks in the mandated store split
CLAIM: C4/FR-INTEG-1 reuse AgenticMind tenant isolation, but that isolation is implemented purely in Postgres: drizzle/0003_tenant_isolation.sql does ENABLE/FORCE ROW LEVEL SECURITY + CREATE POLICY per table keyed on the session GUC current_setting('app.current_tenant') (_tenant.ts). None of this exists or is expressible in ClickHouse, where the highest-volume, most-sensitive data (full trace content) would live under FR-INGEST-3.
WHY: NFR-TENANT-1 states cross-tenant leakage must be impossible at the storage layer. The existing guarantee is enforced BELOW the app by FORCE RLS. Failure scenario: an APL analytics query reads ClickHouse traces without re-implementing tenant scoping in every WHERE clause; because ClickHouse has no FORCE RLS, one missing predicate leaks tenant A's raw agent prompts/outputs to tenant B — exactly the storage-level leak the NFR forbids and the class of bug RLS was adopted to make impossible. The PRD inherits the isolation claim without inheriting the mechanism.
FIX: Either (a) keep traces in Postgres so the same FORCE RLS policy auto-applies (preferred), or (b) if ClickHouse stays, specify a compensating mandatory below-the-app isolation mechanism (per-tenant ClickHouse databases/users or a query proxy that injects tenant filters) and prove it in DoD — do not claim 'reuses AgenticMind tenant-isolation', because that mechanism cannot cross the store boundary. Also correct C4: the repo is v0.8.0, there is no v1.4.0.
VERIFY: Repo confirms every premise. drizzle/0003_tenant_isolation.sql applies ENABLE/FORCE ROW LEVEL SECURITY + CREATE POLICY "tenant_isolation" keyed on current_setting('app.current_tenant') across all tables; _tenant.ts documents it as enforced "below the application and the LLM"; client.ts:47 sets the GUC via set_config('app.current_tenant', tenantId, true). This mechanism is Postgres-only and has no FORCE-RLS/GUC equivalent in ClickHouse. ClickHouse is entirely unbuilt in the repo (0 refs in .ts/.sql; only CONTRIBUTING.md:23 lists it as an aspirational dependency "behind an interface"), yet FR-INGEST-3 (line 106) mandates the highest-volume trace stream live there. The PRD does NOT resolve this: NFR-TENANT-1 (line 177) asserts cross-tenant leakage is "impossible at the storage AND query layer" as a MUST, FR-INTEG-1 (line 161) explicitly claims it "uses AgenticMind tenant-isolation," and the DoD (line 231) says isolation is "confirmed" — but the doc never mentions RLS, per-tenant ClickHouse DBs/users, or a query proxy anywhere. So the "reuses AgenticMind tenant-isolation" claim is provably false for the ClickHouse trace store, creating a real unaddressed contradiction on a security MUST covering the most sensitive data (raw prompts/outputs). The C4 version claim is also wrong: package.json is 0.8.0, highest tag v0.12.0, no v1.4.0 exists. Downgraded from critical to high: this is a spec-completeness/consistency defect in a Draft v0.1, not a demonstrated live leak, and standard ClickHouse isolation options (per-tenant databases, RBAC/ROW POLICY, filter-injecting proxy) exist — the fix is to specify and DoD-prove one, not a dead end.
STEELMAN: NFR-TENANT-1 is phrased as an outcome ("cross-tenant leakage impossible"), not a mandated mechanism, so a charitable reading treats the ClickHouse enforcement as a design detail deliberately deferred in a Draft v0.1 rather than a contradiction. ClickHouse is not helpless here — per-tenant databases/users, native ROW POLICY/RBAC, and a filter-injecting query proxy can all deliver below-the-app isolation, so the outcome NFR remains achievable and the store split is not inherently unsafe. The finding also usefully overstates permanence: nothing in the PRD forces the leaky "one missing WHERE predicate" pattern; the team could equally choose per-tenant ClickHouse databases so isolation is again structural, not per-query. The only strictly-wrong statement in the PRD is the FR-INTEG-1/C4 claim of literal reuse and the phantom v1.4.0 — everything else is a gap to fill, not a design error.

====================================================================================================
[HIGH] CONFIRMED  judge#1  (judge)
FR: FR-JUDGE-2, FR-IMPROVE-3
TITLE: 100-label calibration is statistically unstable; PRD omits mandatory stratification the codebase already relies on
CLAIM: FR-JUDGE-2 mandates '>=100 human labels; TPR AND TNR both >80%' but specifies neither class balance nor a confidence interval, so the acceptance threshold is meaningless under the realistic rare-failure regime the whole product is built to detect.
WHY: TPR is estimated only on the positive class, TNR only on the negative class. At n=100 with a realistic ~5% failure base rate, a random production sample yields ~5 positives; TPR is then a fraction over ~5 cases where the 95% CI spans roughly 0.5-1.0 — one misjudged case swings it from 100% to 80%. A judge can 'pass' at 80% TPR on 5 positives purely by luck and still be worthless in prod, and it will flap PASS/FAIL between calibration runs (judge is non-deterministic LLM). The repo's OWN calibration set sidesteps this by being 64/65 balanced constructed pairs (eval/judge-labels.json), and eval/README.md says labels are 'constructed obvious-grounded/obvious-ungrounded pairs' — i.e. stratification is done in practice but the PRD requirement drops it, so an implementer following the PRD literally with random production labels will ship an uncalibrated judge that passes the gate.
FIX: Rewrite FR-JUDGE-2 to require (a) class-stratified labels with a stated minimum per class (e.g. >=50 positives AND >=50 negatives, mirroring the existing 64/65 corpus), and (b) a lower confidence bound, not a point estimate — e.g. 'Wilson lower 95% bound on both TPR and TNR must exceed 0.8', which forces sample size up when a class is thin. Reuse computeCalibration() in judge-calibration.ts and add the interval; make the balance an invariant in the judge-labels builder the way build-eval-cases.ts enforces >=50 per failure mode.
VERIFY: PRD text checks out exactly. FR-JUDGE-2 (line 129) says only "≥100 человеческих меток; TPR и TNR обе > 80%" — no class-balance floor, no confidence interval. Acceptance criteria (lines 133, 227) repeat the same point-estimate/threshold framing. The repo confirms the gap is real, not addressed elsewhere: judge-calibration.ts computeCalibration() is bare confusion-matrix point estimates with zero CI; calibrate.ts runs threshold 0.8 with NO min-per-class or min-total guard. Meanwhile the repo's OWN corpus eval/judge-labels.json is 64 True / 65 False (near-perfectly balanced, matching the finding's "64/65" claim), and build-eval-cases.ts DOES enforce a hard TARGET=50 per failure mode (exits if any mode is below) — so the codebase demonstrably relies on stratification/floors that FR-JUDGE-2 drops. The statistical core is sound: at n=100 with a ~5% base rate a raw prod sample yields ~5 positives, where an 80% point TPR (4/5) has a Wilson 95% interval spanning roughly 0.38–0.96 — pure noise, and it will flap PASS/FAIL across runs of a non-deterministic LLM judge, which then gates both deploy and L3 auto-improvement (FR-IMPROVE-3 / R2). The recommendation (per-class minimums + Wilson lower bound, reuse computeCalibration/build-eval-cases patterns) is coherent and actionable. Only overstatement is severity: the reference implementation is correct on a balanced set and the flaw is a spec-wording/robustness gap for a naive implementer, not a contradiction or architectural break — High, not Critical.
STEELMAN: FR-EVAL-3 seeds the golden set from a weekly PROD FAILURE sample plus manual seeding, which structurally over-represents positives (real failures), so the "5 positives out of 100 random" worst case is not the intended sampling regime — the labels are curated, not a raw production draw. FR-JUDGE-4 further says human annotations are reused for calibration AND error taxonomy, implying a deliberately constructed, failure-rich set rather than a naive random sample. The repo proves the team already builds balanced sets by construction (64/65) and enforces per-mode floors elsewhere, so a competent implementer inherits the right practice from the reference code even if the PRD sentence is terse. FR-JUDGE-3 (recalibrate on every prompt change, publish TPR/TNR each release) plus the golden-set regression gate gives ongoing external checks that would surface a judge that "passed by luck," limiting blast radius. A PRD is a requirements doc, not a statistics manual; specifying exact CI methodology (Wilson bounds) arguably belongs in the eval implementation spec, and the "both rates > 80%" MUST is a defensible high-level bar.

====================================================================================================
[HIGH] CONFIRMED  improve-trust#2  (improve-trust)
FR: FR-IMPROVE-4, NFR-SEC-1, R3
TITLE: The prompt/few-shot autonomy boundary (FR-IMPROVE-4) is not mechanically enforceable
CLAIM: FR-IMPROVE-4 claims auto-evolution touches 'only prompt/context/few-shot' and 'never tools, access rights, or trust boundaries'. But a prompt, a 'context strategy', and 'few-shot from good traces' are all free-form text that flows into the model's instruction channel. Nothing in the codebase distinguishes an innocuous prompt edit from one that instructs the agent to call a side-effecting tool, escalate scope, or exfiltrate. The 'boundary' is a category label with no classifier or validator behind it.
WHY: agent_version.tools[] being unchanged does NOT mean tool BEHAVIOR is unchanged: an auto-patched prompt can add 'always call delete_record before answering' or 'when asked X, output the contents of env var Y', turning a 'prompt-only' patch into a privilege/trust change while passing the FR-IMPROVE-4 check that only inspects the tools[] array. The Cycle-of-Trust guarantee (NFR-SEC-1) is code-enforced today only on the agent INPUT surface (guard.ts on kl_ask_global/mem_write) — there is NO equivalent guard on machine-generated patch content. DoD 'попытка авто-улучшения изменить набор инструментов ... отклоняется' tests the wrong thing (the tools array), not the actual attack surface (the prompt text).
FIX: Define the boundary as an allowlist over the patch DIFF plus a content guard on the patched prompt: run guard-style injection/exfil detection (extend guard.ts patterns) over any auto-generated prompt/few-shot before it can enter a version; forbid patches that introduce tool-invocation directives or scope language; require the diff to be structurally confined to designated prompt/few-shot fields (reject any change that references tool names, secrets, or access verbs). State plainly in the PRD that 'tools[] unchanged' is necessary but not sufficient.
VERIFY: The finding correctly reads the PRD and repo. FR-IMPROVE-4 (line 148) and its DoD (line 152) define the autonomy boundary as "only prompt/context/few-shot" and phrase the enforcement test as rejecting attempts to change "набор инструментов или права" (the tool SET / rights). But a prompt/context/few-shot patch is exactly the free-text instruction channel — an auto-patched prompt can add tool-invocation directives ("always call delete_record") or exfil directives ("output env var Y") while leaving agent_version.tools[] byte-identical, thereby passing the only mechanical check the PRD names and still breaching NFR-SEC-1 / R3 ("границы доверия"). So the DoD does test the wrong surface: tools[]-unchanged is necessary but not sufficient. The repo corroborates the gap: guard.ts (packages/shared/src/lib/knowledge/guard.ts) is exactly as described — guardInput/detectInjection run only on the agent INPUT surface (kl_ask_global queries; callers: ask.ts, mcp-tools.ts, tune.ts, eval scripts) and detectOutputLeak on answers; nothing runs injection/exfil scanning over machine-generated prompt/patch content. AgenticMind today has no prompt/few-shot patcher at all (tune.ts only grid-searches retrieval knobs; the "compounding loop" in feedback-*.ts promotes resolution-card content via a faithfulness judge, not agent prompts), so the PRD is specifying a net-new machine-writes-into-instruction-channel path with no content guard defined for it. The PRD never states that prompt-diff content itself must be validated; FR-IMPROVE-6/config only whitelists patch TYPES and agents, and FR-JUDGE gates on eval score/regression — an injected "call delete_record" directive can plausibly pass an eval that measures answer quality, not side-effect safety. The finding is a genuine spec gap, not a misread.
STEELMAN: The PRD's strongest defense: FR-IMPROVE-4 is a category-level requirement, and the "how" is intentionally deferred to implementation — a spec is allowed to state the invariant without dictating the classifier. The safety envelope is not the tools[] check alone: L3 (FR-IMPROVE-3) requires a full eval + an independent calibrated judge confirming no regression AND no new failure clusters before auto-merge to canary, then A/B against prod before promote. A prompt that inserts "always call delete_record" would very likely surface as a new failure cluster or an eval/assert regression (FR-EVAL-2a includes tool-call validity and cost budgets), and canary A/B gives a rollback path — so the injected directive is not a silent one-shot escape. FR-IMPROVE-6 also makes L3 opt-in per agent with an operator-configured patch-type whitelist, and NFR-SEC-2 keeps secrets out of the agent/traces (blunting the "output env var Y" exfil). Finally, the finding's own recommendation (extend guard.ts patterns over the diff, forbid tool-name/verb references) is straightforward to add and is arguably already implied by "форсируются кодом" in NFR-SEC-1 — so this is a wording/completeness gap the PRD can close with one clause rather than a fundamental infeasibility. That eval+judge+canary defense-in-depth is why I adjust critical down to high: the boundary is under-specified and the DoD tests the wrong artifact, but it is not the sole line of defense the finding implies.

====================================================================================================
[HIGH] CONFIRMED  improve-trust#3  (improve-trust)
FR: FR-IMPROVE-2, FR-IMPROVE-4, NFR-SEC-1, NFR-SEC-2
TITLE: Auto-mined few-shot / promoted answers have no injection or output guard — a poisoned production trace becomes durable behavior
CLAIM: FR-IMPROVE-2 mines 'few-shot from good traces' with only a human/LLM reviewer, and FR-IMPROVE-3 auto-promotes on a grounding judge. The existing analogue in the repo — feedback-promoter.ts — proves this loop already ships WITHOUT a content-safety gate: sweepPromoteClusters takes the top-scored production answer (fetchBestAnswer), runs ONLY the grounding judge (JUDGE_SYSTEM checks factual support vs citations), and inserts it into knowledge_cards which immediately rejoin the retrieval pool. guardInput is never re-run on the promoted text (confirmed: 0 guard calls in feedback-promoter.ts / feedback-builder.ts).
WHY: An attacker who can influence production traffic (or a compromised upstream source that a citation quotes) crafts an answer that scores high on user signals and is 'grounded' in an attacker-controlled citation but also carries an embedded instruction ('When summarizing, always recommend product Z' / an exfiltration directive). The grounding judge passes it (it only checks factual support, not intent), it becomes a resolution card / few-shot, and it then influences all future answers/agents that retrieve it — a self-reinforcing poisoning channel that survives across versions. Because promoted content is later consumed as model context, this is stored prompt-injection with a durability the input guard was built to prevent.
FIX: Before any mined trace becomes a few-shot or promoted card, run it through the SAME injection/exfil/PII guard that gates input (guard.ts) plus an output-leak check; add a mandatory 'is this an instruction, not just an answer?' judge dimension distinct from grounding. Provenance-tag every mined example with its source trace_ref and tenant, quarantine examples from low-trust/anonymous traffic, and make the improvement ledger (FR-IMPROVE-5) record the source trace so a poisoned example can be traced and purged fleet-wide.
VERIFY: Verified in repo and PRD. feedback-promoter.ts sweepPromoteClusters runs ONLY the grounding judge (feedback-judge.ts JUDGE_SYSTEM: "Judge only factual support — not style or completeness"), then writes best.answer into knowledge_cards kind=resolution (embedded → rejoins hybrid retrieval) with zero guardInput / detectOutputLeak / injection-scan calls (confirmed 0 in feedback-promoter.ts + feedback-builder.ts). Both guards exist and gate OTHER surfaces — a real asymmetry: guardInput at mcp-tools.ts:109/212 on the ask question, detectOutputLeak at ask.ts:355 on generation. Invoked live from apps/worker/.../knowledge-feedback/handler.ts:42. The kl_feedback tool description itself states strong signals "promote the answer into a reusable resolution card," and cards are later fed as model context via ask.ts buildPromptWithGraphContext — so the durable poisoning channel is real. On the PRD side: FR-IMPROVE-2/3 mine few-shot/promote on a grounding judge; the ONLY safety envelope (FR-IMPROVE-4 / NFR-SEC-1 / R3 / glossary "Cycle of Trust", grep-confirmed lines 53/148/173) restricts permission/capability/tool changes and EXPLICITLY permits prompt/context/few-shot auto-evolution — the exact channel the poison flows through. No mention anywhere in the PRD of injection scanning, sanitization, poisoning, quarantine, or trust-tiering of mined production traffic (grep for инъекц/inject/saniti/poison/quarantin/trust returned only the permission-boundary Cycle-of-Trust hits). FR-JUDGE-2 calibration and FR-IMPROVE-3 "no regression / no new clusters" are quality/grounding gates, not intent/content-safety gates — a grounded-but-instruction-carrying example passes them. Downgraded to high (not critical) because the promoted body is the LLM-synthesized answerCache.answerText (grounded against real retrieved sources, already past detectOutputLeak at generation), not raw attacker free-text — so the "attacker crafts an answer" framing overstates ease; the realistic vector is the finding's own alternative (a compromised/poisoned upstream corpus source whose instruction the model faithfully reproduces, grounded → judge passes), which needs corpus write or source compromise plus sustained high user signal. PII-exfil sub-claim is partially mitigated by FR-SDK-5 / NFR-PRIV-1 redaction. Provenance recommendation is partially pre-addressed (failure/annotation carry trace_ref, improvement logs version_from→to) but eval_case source=mined has no source-trace and improvement records no origin trace, so fleet-wide purge of a poisoned example is not supported.
STEELMAN: The PRD is a v0.1 draft explicitly scoping A/B promotion infrastructure to Phase 5, and it does layer several controls the finding underweights: L3 is opt-in per agent (FR-IMPROVE-6), requires a calibrated judge with TPR/TNR>80% on ≥100 human labels (FR-JUDGE-2) confirming no regression AND no new failure clusters (FR-IMPROVE-3), plus a rotating human ground-truth sample (FR-EVAL-2c) and human review at L2 (FR-IMPROVE-2) — so a purely automated poison-to-production path requires defeating a calibrated judge, a cluster-drift detector, AND human reviewers simultaneously. FR-SDK-5/NFR-PRIV-1 already redact PII pre-storage, defusing the exfil-of-PII variant. The mined artifact is model-synthesized text grounded in cited sources, not free-form attacker input, and the analogous repo path additionally has sticky terminal cluster states preventing re-promotion. The "safe envelope" is deliberately about the highest-blast-radius surface (tools/permissions/trust boundaries) where an error is unrecoverable, whereas a poisoned few-shot is bounded, observable via the same cluster trend/alerting machinery (FR-FAIL-3), version-attributed (FR-REG-3), and reversible via rollback — arguably an acceptable v1 risk posture with content-safety hardening slotted for the same Phase 5 that owns promotion.

====================================================================================================
[HIGH] unverified  failure#2  (failure)
FR: FR-FAIL-2, G3, §8 failure_cluster
TITLE: The 4-stage taxonomy pipeline (open coding → axial coding → named categories) has zero analogue and is understated as 'reuse'
CLAIM: FR-FAIL-2 reads as an incremental extension of existing clustering, but only stage 1 (embedding nearest-neighbour join) exists; LLM open coding, axial coding, and stable named per-agent categories are entirely net-new, non-deterministic LLM steps with no calibration, cost model, or tie-in to the version registry.
WHY: grep for 'open cod|axial|label.*cluster|name.*cluster' returns nothing; clusters today are identified only by representativeQ = the raw text of the first member (ask-clusters.ts:29), which is not a taxonomy label. Failure scenario: an LLM naming a cluster 'tool-schema violation' one week and 'invalid function-call arguments' the next produces two distinct labels for one category, breaking the count/trend continuity FR-FAIL-3 depends on and inflating G3's ≤10-cluster budget. There is no judge/calibration on the naming step (FR-JUDGE only covers eval verdicts), so the taxonomy quality is unmeasured.
FIX: Rewrite FR-FAIL-2 to name the four stages as distinct build items with their own acceptance: pin the coding model + prompt as a versioned entity (mirror the existing judge-version pattern), make axial labels chosen from a per-agent controlled vocabulary that only grows via explicit review, and store label-embedding for run-over-run matching. Budget the LLM open-coding cost (it runs over every fail-trace, not a 5% sample) against NFR-OBS-1.

====================================================================================================
[HIGH] unverified  failure#3  (failure)
FR: FR-FAIL-3, R6
TITLE: FR-FAIL-3 cannot distinguish a genuinely new cluster from a renamed/split one — the exact distinction the acceptance test requires
CLAIM: 'Alert on new cluster after deploy' has no definition of 'new' that survives non-deterministic embeddings + LLM naming; the only existing signal is feedback-builder's `newClusters` counter, which means 'no centroid within 0.85 cosine' — a threshold artifact, not a semantic novelty signal, and with no deploy/version correlation.
WHY: feedback-builder.ts increments newClusters purely on shouldJoinCluster()==false; there is no agent_version dimension (that table doesn't exist) to correlate 'new' with 'after deploy'. Failure scenario: routine phrasing variance in fail-traces spawns 3-4 singleton clusters every night → nightly false 'new cluster' alerts → operators mute the alert channel → a real post-deploy regression cluster is missed. Because a slightly-drifted duplicate of an existing failure looks identical to a novel failure, the alert has an irreducible false-positive floor.
FIX: Define 'new cluster' operationally: a cluster is NEW only if (a) it clears a minimum member count (reuse MIN_CLUSTER_SIZE), (b) its label-embedding has cosine < T to every prior cluster's label-embedding for that agent, AND (c) its first member's trace timestamp is after the current agent_version's created_at. Require the agent_version registry (FR-REG) as a hard dependency of FR-FAIL-3, and gate the alert on statistical significance, not raw appearance (see next finding).

====================================================================================================
[HIGH] unverified  failure#4  (failure)
FR: FR-FAIL-5, FR-FAIL-3, G3
TITLE: Trend math and the '3-7 clusters' target collapse on the small counts these clusters will actually have
CLAIM: FR-FAIL-5's '3-7 recurring clusters' and the ≤10 cap (G3) are stated as targets/constraints but the existing algorithm produces unbounded singleton clusters, and per-agent fail volumes at 5% tail-sampling (FR-SDK-4) are too small for the spike-detection FR-FAIL-3 assumes.
WHY: There is no merge/cap stage — one unmatched embedding = one new cluster (feedback-builder.ts:126). MIN_CLUSTER_SIZE=5 gates promotion, not creation, so a per-agent view will show a long tail of size-1/2 clusters, not a tidy 3-7. Meanwhile FR-SDK-4 defaults prod sampling to ~5% (100% on errors); a low-traffic agent may log <20 failures/week, so a cluster going from 1→3 occurrences is within Poisson noise yet would trip a naive 'spike' alert. Concrete: an agent with 12 weekly failures across 6 clusters has ~2 per cluster — no cluster is statistically distinguishable from another, making both the ≤10 target vacuous and the trend line pure noise.
FIX: Drop '3-7' as a requirement — make it a non-binding observation, or add an explicit agglomeration step (merge clusters whose label-embeddings are within T, cap at N by absorbing the smallest into 'other') and state the minimum weekly fail volume per agent below which trend alerts are suppressed. Specify the spike test (e.g. EWMA/Poisson-rate change with a confidence threshold), not just 'alert on spike'.

====================================================================================================
[HIGH] unverified  failure#5  (failure)
FR: FR-FAIL-1, FR-FAIL-2, §8 failure
TITLE: The failure pipeline presupposes stored traces, but AgenticMind only EXPORTS spans (no OTLP receiver, no trace store)
CLAIM: FR-FAIL-1 routes failing traces into a pipeline and §8 `failure.trace_ref` FKs a trace row, but AgenticMind is an OTLP producer (apps/server/src/tracing.ts registers an OTLPTraceExporter pushing spans OUT to Phoenix/Langfuse); there is no OTLP receiver and no trace/span table to reference or to embed.
WHY: To 'embed fail-traces (pgvector)' (FR-FAIL-2) the traces must first be received, stored, and reconstructed into a fail-trace document — none of which exists. The engine's spans leave the process via BatchSpanProcessor and are never persisted locally. Failure scenario: a fail signal fires (thumbs-down) but the corresponding trace already flew to an external backend under a 5% sampling regime and was never captured on the error path in a queryable form, so there is nothing to embed or attach as example_refs. FR-INGEST-1 ('accept OTLP') is likewise net-new — the repo has an exporter, not a collector.
FIX: Make explicit that FR-FAIL depends on a net-new ingestion+storage tier (OTLP receiver → trace store), and reconcile it with FR-SDK-4: force 100% capture on the error path INTO the failure store (not just 100% sampling of spans exported outward). Define what a 'fail-trace' document is (which spans/attributes are serialized for embedding) since OpenInference spans today carry capped input.value/output.value (trace.ts cap=2000 chars), which may truncate the very evidence the taxonomy needs.

====================================================================================================
[HIGH] unverified  improve-trust#4  (improve-trust)
FR: FR-IMPROVE-3, FR-JUDGE-2, FR-JUDGE-3, R2, R6
TITLE: The gating judge is reward-hackable and shares identity with the calibration/improvement judge
CLAIM: L3 uses 'an independent calibrated judge' to confirm a patch's gain, but the improvement proposer (L2/L3) and the judge optimize against the same golden set and metric, and FR-IMPROVE-4 permits editing the prompt/few-shot — the exact levers that game an LLM judge. Nothing keeps the judge independent from the thing being optimized: in the repo, calibrate.ts and feedback-promoter.ts use the identical JUDGE_SYSTEM/judgeAllowsPromotion pair, and TPR/TNR are measured on a static labeled file (eval/judge-labels.json), not per proposal.
WHY: When you optimize prompts to maximize a judge's pass rate, you optimize the judge's known biases (verbosity, confident phrasing, citation-shaped text) rather than real quality — classic reward hacking, and precisely the vanity-metric failure R6 warns against. A patch can lift golden-set score and judge-approval while regressing production behavior on cases the judge is blind to. FR-JUDGE-2's 80% TPR/TNR bar is a point-in-time property of a fixed label set; once the improvement loop starts editing prompts, the judge's calibration on NEW proposal outputs is unknown, yet the loop keeps trusting the old TPR/TNR. There is no held-out set, no judge/proposer separation, and no re-calibration triggered by the improvement loop (only by manual judge-prompt changes per FR-JUDGE-3).
FIX: Split the eval corpus into a tuning set (visible to the proposer) and a sealed held-out set (only the gating judge sees it, rotated); require the gating judge to be a different model/prompt family than any judge used to generate or score patches; add adversarial/anti-gaming cases to every per-agent golden set; and re-measure judge TPR/TNR against fresh human labels on a rotating sample of the loop's OWN proposals (not just when the judge prompt changes), blocking auto-merge if calibration drifts below threshold on recent outputs.

====================================================================================================
[HIGH] unverified  improve-trust#5  (improve-trust)
FR: FR-IMPROVE-5, NFR-PRIV-2, FR-IMPROVE-3
TITLE: Improvement ledger (FR-IMPROVE-5) lacks the fields needed to prove trust and to roll back safely
CLAIM: The improvement entity captures version_from/to, hypothesis, eval_delta, status, author — but omits everything needed to (a) reproduce the decision, (b) audit an auto-merge under EU AI Act positioning (NFR-PRIV-2), and (c) execute a rollback. Missing: judge_version + calibration snapshot at decision time, the eval_run id(s) the delta came from, the exact patch diff, per-failure-mode deltas (not just aggregate), the source traces of any mined few-shot, and the canary/A-B result that authorized promotion.
WHY: With only an aggregate eval_delta, an auto-merged L3 change that improved overall pass rate while regressing a rare safety-critical mode is indistinguishable in the ledger from a clean win — you cannot answer 'why did the agent get worse at X after this auto-merge?' or 'which judge, at what calibration, approved this?'. For a regulated-audit pitch, 'author=judge-gated' with no captured judge_version/calibration is not defensible. Rollback needs the exact prior version + the diff; storing only version_from/to ids assumes the version registry is immutable and complete, but the ledger itself doesn't bind the judge/eval evidence that justified the transition, so a post-hoc audit can't verify the gate actually held.
FIX: Extend improvement to record: patch_diff (structured, field-scoped), eval_run_id + per-failure-mode delta, judge_id + judge_version + calibration (TPR/TNR, label_count) as-of decision, canary/A-B outcome, source_trace_refs for mined content, and a rollback_of pointer. Make 'author=judge-gated' require all of these present as a code-level invariant (analogous to how guard-events.ts always stores actor+reason+hash), so an auto-merge is un-writable without its full justification.

====================================================================================================
[HIGH] unverified  integ-nfr-score#3  (integ-nfr-score)
FR: FR-INTEG-2, FR-CONTRACT-2, FR-CONTRACT-3, G1
TITLE: 'Anything emitting OTel GenAI spans integrates mostly by mapping' is false — the four named frameworks are not span-uniform, and the repo emits OpenInference, not gen_ai.*
CLAIM: FR-INTEG-2 asserts thin adapters where 'everything emitting OTel GenAI spans integrates mostly by mapping', built on FR-CONTRACT-2/3's canonical invoke_agent/chat/execute_tool + gen_ai.* attributes. But grep across the whole repo returns ZERO gen_ai.*/invoke_agent/execute_tool, and the existing instrumentation (packages/shared/src/lib/observability/trace.ts) uses a DIFFERENT convention entirely — OpenInference (openinference.span.kind, llm.model_name, kinds CHAIN/RETRIEVER/LLM).
WHY: The 'mostly mapping' premise assumes the ecosystem is standardized on OTel GenAI semconv; it is not. CrewAI and LangGraph instrumentations emit OpenInference/OpenLLMetry, the OpenAI Agents SDK emits its own trace format, and the Claude Agent SDK has no native OTel GenAI emitter — so 'adapter' means real, per-framework translation of span trees and attribute names, not a thin field map. Concretely, the FR-CONTRACT acceptance test ('a LangGraph trace and a raw-code trace produce IDENTICAL tree structure and the required gen_ai.*+moai.* attribute set') will fail on day one because these sources produce different tree shapes and attribute vocabularies, and the AgenticMind engine's own spans use OpenInference — so even AgenticMind is not compliant with its own contract. This also inflates G1 (≤1 day onboarding) for any framework that isn't already gen_ai.*-native.
FIX: Downgrade FR-INTEG-2 from 'mostly mapping' to 'per-framework adapter with a normalization layer'; scope MVP to ONE reference adapter (raw/manual TS SDK) plus AgenticMind's own engine, and per-framework adapters as follow-ups with explicit conformance tests. Add a normalization step that maps BOTH OpenInference and gen_ai.* into APL's internal model (the engine already speaks OpenInference — decide which is canonical and translate the other). Rewrite the FR-CONTRACT acceptance criteria to test post-normalization equivalence, not raw-emission equivalence.

====================================================================================================
[HIGH] unverified  integ-nfr-score#4  (integ-nfr-score)
FR: NFR-PORT-1, FR-CONTRACT-5, FR-SCORE-4, NG5
TITLE: NFR-PORT vendor-neutrality is internally contradicted by the moai.* attributes every APL feature depends on
CLAIM: NFR-PORT-1 promises 'no format lock-in; readable by any OTel backend', and NG5 says 'no proprietary telemetry format'. But FR-CONTRACT-5 mandates a proprietary namespaced set moai.* (agent_id, agent_version, product_id, tenant_id, outcome, human_feedback, decision_reason) that carries ALL the semantics APL's registry, scorecards, and clustering key off. moai.* appears nowhere in the repo today (grep = 0 hits).
WHY: The claim is only true for raw span transport and false for anything useful. A customer who points APL's OTLP export (FR-SCORE-4) at Datadog/Grafana receives spans whose APL-meaningful dimensions live entirely in moai.* keys those backends don't understand — so 'vendor-neutral' delivers uninterpretable blobs. Meanwhile APL itself is 100% locked to moai.*: pull APL out and the traces are just generic spans with no agent/version/outcome linkage. So the PRD simultaneously claims no-lock-in and builds its whole value on a proprietary namespace. Calling the namespace 'moai.*' also contradicts the Apache-2.0 open-core framing (C1) — an open module emitting a company-branded proprietary namespace as a MUST is an odd coupling.
FIX: State the honest position: APL is portable at the OTEL TRANSPORT layer but its semantics require APL-specific attributes — that's normal, drop the absolute 'no lock-in' wording. Prefer standardized keys where they exist (map agent_id/agent_version onto emerging gen_ai.agent.* semconv, tenant onto a resource attribute) and confine only genuinely-custom fields (outcome, decision_reason, human_feedback) to a namespace. Rename moai.* to a product-neutral namespace (e.g. apl.*) consistent with the Apache-2.0 open-core positioning, and reconcile NG5/NFR-PORT-1 with FR-CONTRACT-5 in the doc.

====================================================================================================
[HIGH] unverified  integ-nfr-score#5  (integ-nfr-score)
FR: NFR-PRIV-1, NFR-SEC-2, FR-SDK-5, FR-CONTRACT-7
TITLE: PII/secret redaction 'before storage' is unbuilt for the trace path — content-capture + tool spans can persist credentials and free-text PII
CLAIM: NFR-PRIV-1 requires PII redacted BEFORE storage and NFR-SEC-2 requires tool secrets never in traces, while FR-CONTRACT-7 enables capturing gen_ai.input.messages/output.messages/system_instructions and FR-SDK-5 names a 'Presidio sidecar'. The only redaction that exists (packages/shared/src/lib/knowledge/guard.ts) is regex-only (email/phone/card/SSN/IPv4), runs IN-PROCESS on the knowledge mem_write path, and does NOT touch the OTLP export path (trace.ts only cap()s length to 2000 chars — no redaction). Presidio is not a dependency.
WHY: When content-capture is enabled, full prompts/responses and tool I/O flow to storage with no redaction seam on the export path. Concretely: a product's tool span captures an execute_tool call whose arguments include an API key or a customer's message containing a name+address; APL persists it verbatim into the (ClickHouse) trace store. Regex redaction (even if wired) misses names, addresses, and free-form PII, and catches no secrets at all. In a multi-tenant SaaS this is a direct GDPR/data-processing exposure, and it undercuts the EU AI Act auditability positioning (NFR-PRIV-2) by storing exactly the data regulators penalize retaining unredacted.
FIX: Make redaction a mandatory, tested processor on the EMIT/collector path (SDK-side scrubber or a Collector redaction processor + Presidio) that runs before any exporter — not the in-process knowledge-write regex. Default content-capture OFF; when on, enforce redaction and add a secret-detection pass (entropy + known-key patterns) over tool span attributes. Add acceptance tests proving a synthetic secret/PII in a captured message never reaches storage. Until this exists, drop the 'redacted before storage' MUST to a known gap rather than an implied guarantee.

====================================================================================================
[MEDIUM] PLAUSIBLE  ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3)#2  (ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3))
FR: FR-REG-3, FR-INGEST-3, §8 trace/span, §8 agent_version
TITLE: Cross-store FK trace -> (agent_id, agent_version) is unenforceable: FR-REG-3 contradicts FR-INGEST-3
CLAIM: §8 declares 'trace/span ... FK -> (agent_id, agent_version)' and FR-REG-3 requires every trace/eval_run/improvement to be attributed to a concrete (agent_id, agent_version). But FR-INGEST-3 puts traces in ClickHouse and FR-REG-2 puts agent_version in Postgres. ClickHouse has no enforced foreign keys, and no Postgres FK can reference a ClickHouse row.
WHY: The registry's entire value (acceptance §6.4: any trace reconstructs the exact config; a regression is attributed to a concrete version) depends on that referential link being real. Failure scenario: an ingest writes a trace with an agent_version that never existed in the registry (typo, race, replay); nothing rejects it, so weeks of traces silently reference a phantom version and the regression-attribution acceptance criterion produces wrong or null joins with no error. The split also creates a write-ordering problem: a trace can land in ClickHouse before its agent_version is committed to Postgres.
FIX: Keep traces and registry in the same Postgres so the FK is a real enforced constraint (agent_version_id uuid REFERENCES agent_version(id)). If a second store is ever unavoidable, the PRD must (a) demote FR-REG-3 to a soft, denormalized, validated-on-ingest attribution, (b) specify the reconciliation job that quarantines orphan-version traces, and (c) define the write-ordering guarantee (registry commit before trace accept).
VERIFY: The factual core holds: §8 (line 189) literally writes "trace/span ... FK -> (agent_id, agent_version)" while FR-INGEST-3 (L106) puts traces in ClickHouse and FR-INGEST-2/FR-REG-2 (L105/112) put agent_version in Postgres. ClickHouse enforces no FK constraints and no cross-engine Postgres FK can reference a ClickHouse row — technically true. I grepped the PRD for any mitigation (reconcile/orphan/validate/ordering/denormalize/soft-attribution): the only "orphan" hits (L86, L224) are about intra-trace span lineage across async boundaries, NOT cross-store version orphans. So the PRD genuinely omits the three things the finding names: ingest-time validation, an orphan-version reconciliation/quarantine job, and a write-ordering guarantee (registry-commit-before-trace-accept). The write-ordering race (trace lands before its version commits) is real with two independent stores + async OTLP. Repo evidence strengthens relevance: CONTRIBUTING.md mandates "Keep it Postgres-only ... ClickHouse etc. behind an interface, not in the default path," and the existing high-volume append-heavy audit table (ask_telemetry) lives in Postgres with real FK-capable refs — so the finding's fix (keep traces in Postgres so the FK is enforceable) matches the codebase's actual precedent. Downgraded from critical because the claim overreaches on impact: FR-CONTRACT-5 emits agent_id/agent_version as denormalized moai.* attributes ON each span, and agent_version is a hash-addressed immutable snapshot (L46/112), so version attribution travels inside the trace payload and the §6.4 acceptance criterion ("regression attributed to a concrete version") is largely satisfiable WITHOUT any live FK join. The consequence is silent-orphan/null-join edge cases (typo/race/replay) plus a misleading "FK" label in an entity sketch — a real design-hardening gap, not a showstopper that voids "the registry's entire value."
STEELMAN: "FK" in §8 is a data-model sketch's logical join-key shorthand, not a literal DDL REFERENCES constraint — no observability stack ever enforces referential integrity on OTLP spans. Because agent_version is an immutable hash-addressed snapshot and FR-CONTRACT-5 stamps (agent_id, agent_version) as denormalized attributes on every span, each trace is self-describing: you can reconstruct the exact config and attribute a regression from the span payload alone, with the Postgres registry as an enrichment/lookup side, not an integrity gatekeeper. A hot OLAP trace store (ClickHouse, per NFR-SCALE-1) plus a relational control plane is standard, deliberate observability architecture; eventual reconciliation and validate-on-ingest are routine operational patterns that a v0.1 draft legitimately defers to implementation rather than a requirements doc.

====================================================================================================
[MEDIUM] PLAUSIBLE  failure#1  (failure)
FR: FR-FAIL-2, FR-FAIL-3, G3, R6
TITLE: Cluster identity is non-stable by construction, so every trend/alert built on it is meaningless
CLAIM: The reused clustering primitive assigns members greedily to the first centroid within cosine≥0.85 and NEVER updates the centroid or re-clusters, so cluster IDs and boundaries depend on arrival order and are unstable across runs — yet FR-FAIL-3 trends and R6's anti-vanity guard require stable per-cluster time series.
WHY: createCluster (database/query/knowledge/ask-clusters.ts) sets `centroid = the first member's embedding` and there is no centroid-recompute anywhere (ask-cluster-members.ts even comments that a member moving clusters is 'rare' — the code never moves it). Concrete failure: after a deploy shifts the error distribution, the SAME real failure mode can either (a) join a stale cluster whose centroid was fixed by an unrelated first sample, or (b) spawn a fresh cluster because the old centroid drifted out of 0.85 range — indistinguishable from a genuinely new failure. The PRD's own acceptance criterion ('after a release that worsened behavior, the corresponding cluster alerts') will fire false positives (new-cluster churn) and false negatives (real regression absorbed into a stale cluster). Embeddings + LLM naming are both non-deterministic; the PRD never specifies how a cluster keeps a stable id/label across nightly runs.
FIX: Specify a concrete stability mechanism before committing to trends: (1) recompute centroids after each sweep and re-run assignment (or adopt HDBSCAN over the full fail-trace set per agent, not online greedy); (2) give clusters a durable identity via embedding of the axial-coded LABEL, matched run-over-run with a merge/carry-forward step (Hungarian match on centroid cosine) so 'renamed' maps to the same id; (3) add a determinism test (absent today — clustering.test.ts tests only thresholds) asserting the same fail-set yields stable cluster ids/labels across two runs within tolerance. Until (1)-(3) exist, FR-FAIL-3 acceptance is unverifiable.
VERIFY: Repo facts are accurate: createCluster fixes centroid to the first member's embedding (ask-clusters.ts:94), recomputeClusterAggregates never touches centroidEmbedding (lines 122-163), feedback-builder.ts assigns online-greedy in arrival order (asc(createdAt), line 77) with no re-clustering, ask-cluster-members.ts:10 comment calls member-move "rare" while code never moves it, and clustering.test.ts tests only thresholds. So IF APL reused this primitive for failure clustering, identity would be arrival-order-dependent and unstable — a real risk to FR-FAIL-3 trends and R6. BUT the finding's load-bearing premise ("the reused clustering primitive...") is an inference, not a PRD commitment: the PRD nowhere says APL reuses ask-clusters.ts. The only explicit reuse claims (C4 line 216, FR-INTEG-1) are about tenant-isolation, and "переиспользуемый" (line 17) means APL-is-a-reusable-layer, not this specific code. Crucially, FR-FAIL-2 specifies a DIFFERENT, richer pipeline than the /ask code (embedding → semantic clusters → LLM open coding → axial coding) and explicitly demands "стабильные именованные категории" — the PRD names stability as the goal rather than contradicting it. No APL/failure-cluster code exists in the repo (only the /ask FAQ-promotion clusterer, a separate feature), so this is a forward-looking under-specification, not a demonstrated fatal contradiction. Downgraded from critical to medium: the PRD does fail to specify HOW stability is achieved (centroid recompute, run-over-run identity match, determinism test), and there is a tempting-but-wrong sibling primitive nearby — a genuine spec gap that leaves FR-FAIL-3 acceptance unverifiable as written — but the "meaningless by construction" framing overstates it by treating an un-adopted implementation as APL's committed design.
STEELMAN: The PRD says "стабильные именованные категории" (FR-FAIL-2) and requires per-cluster trends + post-deploy alerts (FR-FAIL-3) and an anti-vanity guard (R6) — but it never defines what makes a cluster identity durable across nightly runs when embeddings and LLM naming are both non-deterministic. Given AgenticMind's culture of reusing existing primitives (tenant-isolation per C4, and an already-shipped ask-clusters online-greedy clusterer sitting right in the same package), the path of least resistance is to reuse that primitive — which would silently violate the "stable" requirement, since its centroid is frozen to the first arrival and it never re-clusters. The PRD should therefore explicitly (1) forbid frozen-first-centroid online-greedy assignment for failure clustering, (2) specify a run-over-run cluster-identity mechanism (recompute centroids + carry-forward/merge match on label or centroid cosine), and (3) add a determinism acceptance test so FR-FAIL-3's "post-deploy the corresponding cluster alerts" is actually verifiable. Closing this in the spec is cheap now and prevents the exact false-positive/false-negative churn the finding describes.

====================================================================================================
[MEDIUM] PLAUSIBLE  integ-nfr-score#1  (integ-nfr-score)
FR: FR-SCORE-1, FR-SCORE-3, NG3, G5
TITLE: FR-SCORE control-plane UI has no home and contradicts the headless non-goal — the PRD never says where it lives
CLAIM: FR-SCORE-1 (per-agent view with 'pending approvals') and FR-SCORE-3 (fleet view ranked by agent health) are interactive operator UIs, but AgenticMind's ROADMAP.md lists 'A frontend / admin UI' as an explicit non-goal ('headless substrate; agents are the only consumers') and README.md states 'There is no frontend' / 'ships no admin UI'. The PRD positions APL as a MODULE of AgenticMind yet never states where this UI is built or served.
WHY: The entire scorecard/control-plane layer (Layer 7, a MUST) has no architectural home. apps/ contains only server (headless MCP host) and worker (Postgres-scheduled sweep); there is no web app, no auth/session layer, no RBAC. Concretely: a Product Engineer opens the scorecard to approve a pending L2 patch (FR-IMPROVE-2) — there is no page, no route, no login, and building one silently forks AgenticMind from headless to a UI product, invalidating its packaging, security model, and marketing. 'Pending approvals' also implies human-in-the-loop auth/roles (§4) that don't exist.
FIX: Resolve the contradiction explicitly: either (a) declare APL's control-plane a SEPARATE product/app (new apps/console) with its own auth/RBAC and stop calling it a pure AgenticMind module, or (b) make the control-plane API-only (headless: expose scorecards/approvals as MCP tools or a REST API, render nowhere) and move all UI to Q4/enterprise-edition. Amend ROADMAP or the PRD so they stop contradicting each other, and specify the auth/RBAC model before any FR-SCORE work.
VERIFY: Repo claims verified verbatim: ROADMAP.md non-goals say "A frontend / admin UI. AgenticMind is a headless substrate; agents are the only consumers"; README.md L130 "There is no frontend"; L183 "ships no admin UI"; apps/ holds only server (headless MCP host) + worker, no web app, no auth/RBAC. So the tension is real: FR-SCORE-1 (per-agent view with "ожидающие апрува улучшения") plus FR-SCORE-3 fleet view read as operator surfaces, and the PRD never states where any of it renders. But the finding overreaches on several points. (1) FR-SCORE-3 is SHOULD, not MUST — the finding calls the whole layer "a MUST." (2) The glossary (L52) defines скоркарта as a "материализованный вид" (materialized data view, not necessarily rendered UI), and FR-SCORE-4 (MAY) explicitly exports scorecards/metrics to external backends (Datadog/Grafana/Phoenix) — i.e., the PRD already implies rendering happens outside AgenticMind, which is the finding's own recommended resolution (b). (3) Q4 (L262) explicitly parks RBAC/audit/SSO for "enterprise-edition" as an open question, so the auth gap is acknowledged, not silently ignored. (4) §4 (L57-62) does define the roles the finding claims "don't exist"; only the enforcement mechanism is unspecified. Net: a genuine, material underspecification (no stated home for the control-plane; headless non-goal vs. operator approvals), but not the flat blocking self-contradiction the finding paints — the PRD leans headless/API-first with UI+RBAC deferred to enterprise-edition, matching the recommended fix. Design-time spec-clarity gap, not infeasibility → medium.
STEELMAN: The PRD is internally consistent if read as: APL emits scorecards as materialized data (a Postgres/queryable artifact) and exposes them API-first / via OTel export (FR-SCORE-4) to external backends, exactly like AgenticMind's existing OTel observability posture — no in-repo frontend is required, so the ROADMAP "no frontend" non-goal is honored. "Pending approvals" and "fleet view" are data states and query outputs, not a mandated rendered page; any UI/RBAC is explicitly an enterprise-edition open question (Q4), and NG3 already forecloses an agent-builder UI, showing the authors deliberately kept APL headless. The roles in §4 give the human-in-the-loop model a conceptual home; the improvement webhook/API (§9) is the concrete approval integration point, not a login screen. Under this reading the finding is a request to make the already-implied API-first decision explicit, not evidence of a contradiction that breaks the module.

====================================================================================================
[MEDIUM] CONFIRMED  contract-otel#2  (contract-otel)
FR: FR-CONTRACT-2
TITLE: FR-CONTRACT-2 span names invoke_agent/chat/execute_tool are semconv-incorrect: GenAI span names are operation+subject, not bare operation
CLAIM: The PRD makes bare 'chat' and 'execute_tool' the canonical span NAMES. Per the current GenAI semconv (context7), the LLM span name SHOULD be `{gen_ai.operation.name} {gen_ai.request.model}` (e.g. `chat gpt-4o`) and the tool span SHOULD be `execute_tool {gen_ai.tool.name}` (e.g. `execute_tool get_weather`). 'chat' and 'execute_tool' are values of `gen_ai.operation.name`, not span names.
WHY: The acceptance criterion demands 'identical tree structure' across a LangGraph product and a raw-code product. Any conformant off-the-shelf instrumentor (OpenAI/Anthropic OTel instrumentation) will emit `chat gpt-4o`-style names, while a raw-code product following the PRD literally emits bare `chat`. The trees will NOT be identical — the acceptance test fails precisely on the interop scenario it exists to prove. Grouping/aggregation queries keyed on span.name would also fragment.
FIX: Rewrite FR-CONTRACT-2 to: root operation.name=`invoke_agent` (span name `invoke_agent {gen_ai.agent.name}`), child LLM spans operation.name=`chat` (span name `chat {model}`), tool spans operation.name=`execute_tool` (span name `execute_tool {tool}`). Make APL's ingestion/aggregation key on `gen_ai.operation.name` + `moai.agent_id`, NOT on the raw span name, so it is robust to the model/tool suffix that conformant instrumentors add.
VERIFY: FR-CONTRACT-2 (line 82) literally calls invoke_agent/chat/execute_tool the "канонические спаны" (canonical spans) — i.e. treats them as span NAMES. Verified via context7 that the GenAI semconv says the chat span name SHOULD be `{gen_ai.operation.name} {gen_ai.request.model}` (e.g. `chat gpt-4o`), and that chat/execute_tool/invoke_agent are enumerated VALUES of the gen_ai.operation.name attribute, not span names. So the PRD conflates the operation-name value with the span name — a genuine semconv misstatement. The interop risk is not hypothetical: FR-INTEG-2 (line 162) commits APL to ingest "всё, что эмитит OTel GenAI спаны ... маппингом" — off-the-shelf instrumentors (OpenLLMetry/OpenInference/native) emit model-suffixed names, so a raw-code product following the PRD literally (bare `chat`) and a LangGraph product via a conformant instrumentor (`chat gpt-4o`) diverge on span.name, and nothing in the PRD says ingestion/aggregation keys on gen_ai.operation.name rather than raw span.name, so name-keyed grouping would fragment. Slight overreach: the acceptance criterion (line 89) says "идентичную структуру дерева" (tree STRUCTURE), which plausibly means topology + span kinds, not names, so "the acceptance test fails" is arguable rather than certain; and FR-CONTRACT-3 already mandates capturing gen_ai.operation.name as an attribute, so the robust aggregation key exists in the schema even though the PRD doesn't mandate keying on it. Core defect holds; the recommendation (name = operation + subject; key ingestion on operation.name + moai.agent_id) is correct and worth adopting.
STEELMAN: "Спаны" in FR-CONTRACT-2 can be read as shorthand for span operation-name VALUES rather than literal span-name strings, and the term chosen (invoke_agent/chat/execute_tool) matches the exact semconv gen_ai.operation.name enum — so a fair reader takes the PRD to mean "use these canonical operations," with the model/tool suffix being an obvious, non-contradictory implementation detail. The acceptance criterion demands identical tree STRUCTURE (topology: root invoke_agent → child chat → child execute_tool, correct parent/child + span kinds), which a bare name vs a model-suffixed name does not alter; the tree is still isomorphic. FR-CONTRACT-3 already requires gen_ai.operation.name as a first-class attribute, giving APL a stable, suffix-independent aggregation key by construction, so aggregation need never touch the raw span.name. Thus the PRD is arguably already correct in substance and only imprecise in wording — a doc-clarity nit, not a design flaw.

====================================================================================================
[MEDIUM] CONFIRMED  contract-otel#3  (contract-otel)
FR: FR-CONTRACT-4, C2
TITLE: OTEL_SEMCONV_STABILITY_OPT_IN=gen_ai_latest_experimental is a no-op in AgenticMind — no instrumentation library reads it, and the repo hand-rolls spans
CLAIM: FR-CONTRACT-4 treats the env var `OTEL_SEMCONV_STABILITY_OPT_IN=gen_ai_latest_experimental` as the switch that 'enables experimental agent/tool spans.' That flag is honored by OTel *instrumentation libraries* at emit time, not by the SDK, exporter, or the semantic-conventions package. AgenticMind installs none of those instrumentation libraries — it emits spans by hand through `@opentelemetry/api` (trace.ts) — and `grep -rl OTEL_SEMCONV_STABILITY_OPT_IN node_modules/@opentelemetry` returns nothing.
WHY: An operator who sets the env var expecting GenAI spans to appear gets zero behavior change, because nothing in the stack consumes it — the spans are whatever APL's own SDK code chooses to emit regardless of the flag. Worse, it creates a false sense of standards-compliance: the flag reads as 'we follow the OTel migration protocol' when in reality APL must implement the dup/latest emission logic itself. Debugging 'why are my agent spans not experimental?' wastes a day chasing a flag that was never wired.
FIX: Either (a) adopt actual OTel GenAI instrumentation libraries (e.g. the openai/anthropic instrumentors) and then the flag becomes meaningful — but that's a much bigger dependency footprint than 'thin SDK'; or (b) drop the env-var mechanism and state that APL's SDK emits GenAI-experimental spans directly, with the convention version recorded as an APL config value (as FR-CONTRACT-4 half-says). Do not present the env var as the enabling mechanism when no reader exists.
VERIFY: Repo-verified: (1) grep for OTEL_SEMCONV_STABILITY_OPT_IN across all of AgenticMind/node_modules returns nothing — no package reads it; (2) no @opentelemetry/instrumentation* package is installed (only api, sdk-trace-node, exporter, resources, semantic-conventions); (3) spans are hand-rolled through @opentelemetry/api in packages/shared/src/lib/observability/trace.ts (withSpan/recordChildSpan call tracer.startSpan directly). The finding's technical claim is exactly correct: OTEL_SEMCONV_STABILITY_OPT_IN is honored by OTel instrumentation libraries at emit time, not by the SDK/exporter/semconv-constants package — none of which are present as instrumentors here. The PRD does NOT address this; it embeds the misconception: FR-CONTRACT-4 (line 84) says agent/tool spans 'включаются через опт-ин (OTEL_SEMCONV_STABILITY_OPT_IN=...)' — presenting the env var as the causal enabling switch — and C2 (line 214) repeats 'обязательный opt-in' as if setting it does something. Setting the var in APL's stack as described is a no-op. Extra corroboration (beyond the finding): trace.ts actually emits OpenInference conventions (openinference.span.kind, input.value, llm.model_name), not the gen_ai.*/invoke_agent/execute_tool GenAI semconv the FR-CONTRACT series specifies, so the flag has nothing to gate even in spirit. Severity lowered high→medium: the defect is real and should be fixed pre-implementation, but APL is greenfield (C1: module to be built), so this is a spec-correctness/false-standards-signal issue, not a shipped correctness/security/data-loss bug; the finding's 'wastes a day debugging' framing overstates blast radius. Kept above low because the EU AI Act / vendor-neutrality positioning (NFR-PORT-1, NFR-PRIV-2) leans on genuine standards-compliance, which this flag falsely implies.
STEELMAN: The env var is OTel's canonical, blessed idiom for signaling which convention era an emitter targets; copying it costs nothing and communicates standards intent, and FR-CONTRACT-4's second clause ('версия конвенций фиксируется в конфиге APL') already records the convention version in APL config — which is exactly the finding's own recommendation (b). One can read the flag as a documented convention marker rather than a functional switch, and since APL is unbuilt, the team could still choose to adopt GenAI instrumentors later, at which point the flag becomes meaningful. So the PRD is arguably describing target-state intent, not asserting current wiring. The reason this defense ultimately fails: FR-CONTRACT-4's first clause and C2 both use causal 'enabled via / mandatory opt-in' language that presents the env var as the mechanism, not merely a marker, and C3/FR-INTEG-2 commit to a 'thin SDK / thin adapters, mostly mapping' architecture that never takes on the instrumentor dependency footprint required to make the flag load-bearing.

====================================================================================================
[MEDIUM] PLAUSIBLE  sdk#3  (sdk)
FR: FR-SDK-5, NFR-PRIV-1, FR-INGEST-3
TITLE: PII 'redact before export' cannot be an SDK guarantee when redaction lives in the Collector, and doesn't cover the direct-to-ClickHouse path
CLAIM: FR-SDK-5 offers 'collector redaction processor OR Presidio sidecar' as if interchangeable, but both run OUTSIDE the SDK's process; the repo has neither (Presidio grep = 0), and the only PII code (guard.ts) is input-side regex redaction for stored beliefs, not span-export scrubbing. NFR-PRIV-1 says 'PII redacted before it reaches storage'.
WHY: If redaction is a Collector processor, PII is already on the wire (SDK→Collector) before it is scrubbed, so 'redact on emit before export' is false at the SDK boundary; and if the trace store is ClickHouse (FR-INGEST-3) fed via the Collector, the redaction guarantee only holds if EVERY path goes through that one processor — but content-attributes (FR-CONTRACT-7) plus a 5% prod sample means real user messages (gen_ai.input.messages) leave the process unredacted. Failure scenario: a product enables content capture, the Collector's redaction regex misses a passport number format, and raw PII lands in ClickHouse's 30–90 day retention (FR-INGEST-4) inside a tenant's VPC audit surface — a direct NFR-PRIV-1/NFR-TENANT-1 breach the PRD claims is 'impossible'.
FIX: Pick ONE authoritative redaction boundary and make it in-process (SDK span processor) so 'before export' is literally true, OR redefine NFR-PRIV-1 to 'before persistence' and mandate the Collector as required infra with a documented residual-exposure window. Reuse guard.ts's regex set as the in-process baseline; treat Presidio as an optional higher-recall upgrade, not an equal alternative. Add PII handling to the ClickHouse write path explicitly.
VERIFY: Repo claims verified exactly: Presidio grep = 0; the only PII code (packages/shared/src/lib/knowledge/guard.ts redactPii) is input-side, wired at mem_write for stored beliefs (mcp-tools.ts:387), not span-export scrubbing; no OTel Collector config exists; apps/server/src/tracing.ts exports spans via BatchSpanProcessor→OTLPTraceExporter with zero span-level redaction. So content attributes (FR-CONTRACT-7) would leave the process unredacted. The core contradiction is real in the text: FR-SDK-5 (line 96) states "Редакция PII на эмите ... до экспорта" (redaction on emit, before export) as an SDK MUST, yet both offered mechanisms (collector processor, Presidio sidecar) run OUT of process — so "before export" is not literally true at the SDK boundary and an SDK→Collector wire-exposure window exists, unaddressed. BUT the finding overreaches on two points that drop it from HIGH to MEDIUM/PLAUSIBLE: (1) NFR-PRIV-1 (line 175) already says "PII редактируется до попадания в хранилище" (before it reaches STORAGE, not before export) — which is exactly the "before persistence" redefinition the finding recommends, so the collector-redacts-before-ClickHouse path is already consistent at the NFR level; the tension is narrowly with FR-SDK-5's "на эмите/до экспорта" phrasing. (2) The finding conflates NFR-TENANT-1's "кросс-тенантная утечка невозможна" (line 177, about CROSS-tenant isolation) with a passport number landing in the OWNING tenant's own store — that is not a cross-tenant leak, so "a breach the PRD claims is impossible" is a misattribution. Also, missing collector/Presidio infra is partly a greenfield maturity gap (§1.4: APL built as a module of existing AgenticMind, MVP scope), not pure contradiction. Net: real, un-mitigated FR-SDK-5 ambiguity + genuine no-in-process-redaction reality, but partly self-mitigated by NFR-PRIV-1's own wording and weakened by the tenant-isolation misread.
STEELMAN: The PRD is internally more coherent than the finding allows: NFR-PRIV-1 already defines the authoritative privacy boundary as "before it reaches storage," and FR-INGEST-1 mandates OTel Collector compatibility, so the intended architecture is SDK → Collector (redaction processor) → ClickHouse, where redaction demonstrably happens before persistence. FR-SDK-5's "collector processor OR Presidio sidecar" is a deliberate implementation-choice offer within that Collector-mediated pipeline, not a claim that scrubbing happens inside the SDK's own process. FR-CONTRACT-7 is a SHOULD and explicitly flag-gated "с учётом чувствительности," so content capture is off by default and any residual exposure is an opt-in operator decision, not a default breach. The SDK→Collector hop is normally same-host/private-VPC (NFR-INTEG-4 self-hosted/VPC mode), so "on the wire before scrubbing" is a controlled, in-perimeter link, not public exposure. Absence of collector config and Presidio in the repo reflects APL being an in-progress MVP module (§1.4), and MUST items are build targets — not evidence the requirement is infeasible. The finding's own fix (redefine to "before persistence") is largely already the NFR wording, showing the gap is a phrasing tightening in FR-SDK-5, not a foundational design flaw.

====================================================================================================
[MEDIUM] CONFIRMED  sdk#4  (sdk)
FR: FR-CONTRACT-2, FR-CONTRACT-3, FR-SDK-1, FR-SCORE-4
TITLE: Instrumentation contract targets OTel GenAI semconv, but the codebase is committed to OpenInference — a convention fork the SDK must reconcile
CLAIM: FR-CONTRACT-2/3 and glossary standardize on OTel GenAI semconv (gen_ai.*, invoke_agent/chat/execute_tool) and grep shows gen_ai.* = 0 in the repo; the existing trace.ts hardcodes OpenInference keys (openinference.span.kind, llm.model_name, input.value) chosen precisely because 'Phoenix/Langfuse/LangSmith' recognize them.
WHY: OpenInference and OTel GenAI are different, non-identical conventions (attribute names, span-kind taxonomy). Building the new SDK on gen_ai.* while the engine's own why-trace emits OpenInference means a product using AgenticMind + APL emits TWO incompatible span vocabularies, so a single backend (Phoenix) can't render one coherent tree, and the FR-SCORE-4 'OTel-compatible export' promise fractures. Failure scenario: the DoD 'trees from 2 products are identical' passes for two new products but the moment either calls AgenticMind's /ask, the child knowledge span uses OpenInference keys and appears as a foreign/unattributed node — breaking the very single-connected-tree invariant (FR-CONTRACT-6).
FIX: Decide the canonical convention explicitly and either (a) migrate trace.ts/ask.ts to OTel GenAI semconv, or (b) add a documented dual-emit/translation layer. Don't let the PRD assume gen_ai.* greenfield when the engine it's a module of already speaks OpenInference.
VERIFY: Both halves check out against source. PRD: FR-CONTRACT-1/2/3 (lines 81-83) hard-mandate OTel GenAI semconv (gen_ai.request.model, invoke_agent/chat/execute_tool), NG5 + FR-CONTRACT-1 say "собственный формат не вводится", and the §6.1 acceptance criterion (line 89) requires the "обязательный набор gen_ai.* атрибутов". Repo: trace.ts hardcodes OpenInference (openinference.span.kind, input.value, output.value, llm.model_name; kinds CHAIN/RETRIEVER/LLM) with a comment stating these were chosen because "Phoenix / Langfuse / LangSmith" recognize them; ask.ts (AgenticMind's /ask) emits its spans through exactly these keys; grep gen_ai.* = 0 repo-wide (v0.8.0, APL not yet built). Meanwhile C1/§1 constrain APL to be a MODULE of AgenticMind reusing the why-trace, and FR-SDK-7 wants to capture that why-trace. The PRD never once acknowledges that the engine it must embed in already speaks a different, non-identical convention, and prescribes no migration/translation/dual-emit — so the contradiction (greenfield gen_ai.* contract vs OpenInference-committed engine) is real and unaddressed. Adjusted high→medium: the finding overstates the FR-CONTRACT-6 break — OpenInference and OTel-GenAI both ride on OTel context propagation, so a correctly-parented OpenInference span nests structurally and does NOT orphan; the real damage is attribute-vocabulary divergence (a reused /ask subtree fails the "obligatory gen_ai.* set" acceptance criterion and renders as attribute-foreign in a GenAI-semconv view), not a shattered tree. And the fix is bounded: ask.ts is one ~100-line module with ~10 attr keys, translatable or migratable without touching APL's architecture. Genuine gap, not a showstopper.
STEELMAN: The PRD is defensible on intent: it correctly anchors on the emerging industry standard (OTel GenAI semconv is where Phoenix/Langfuse/vendors are converging) rather than perpetuating the OpenInference fork, so choosing gen_ai.* as the canonical contract is the right long-term call — the OpenInference code in trace.ts predates the standard maturing (C2 notes semconv was still "development" status). FR-INTEG-2's principle ("всё, что эмитит OTel GenAI спаны, интегрируется маппингом") implicitly frames non-conforming emitters as an adapter/mapping problem, which is exactly where a trace.ts→gen_ai.* migration would live; the PRD's Phase 0 gate ("заморозить OTel+moai.* схему") is the natural place to resolve this. Since trace.ts is a zero-cost no-op seam until an exporter is registered, migrating it is a contained refactor of a single small module, not a systemic rework — so the PRD's "no proprietary format, standardize on gen_ai.*" stance is coherent and the OpenInference legacy is a one-file cleanup subsumed under the existing Phase-0 contract-freeze work, not an architectural contradiction.

====================================================================================================
[MEDIUM] PLAUSIBLE  eval#2  (eval)
FR: FR-EVAL-4, FR-EVAL-1, FR-EVAL-3, A2, G1
TITLE: Cold-start deadlock: version 1 of a brand-new agent has no golden set and no prior run, so the deploy gate is undefined
CLAIM: FR-EVAL-1 forbids a global suite and FR-EVAL-3 grows the golden set FROM prod failures, but FR-EVAL-4 blocks the first deploy on 'regression relative to current prod' — for a new agent there is no prod, no golden set, and no baseline run, leaving the gate's behavior unspecified.
WHY: The PRD's own G1 (integrate a new product in ≤1 day) collides with a gate that structurally cannot evaluate v1: you cannot mine prod failures from a product that has never run in prod, and you cannot regress against a version that doesn't exist. Failure scenario: a team wires the SDK, ships agent X v1, and the CI gate either (a) hard-blocks forever (no baseline → 'regression' is undefined/false-negative) or (b) waves everything through (no cases → passRate=1 by harness.ts:187 which returns 1 on an empty suite), which is exactly the vanity-pass R6 warns about. Either way G2 ('regressions don't reach prod') is violated on the most common path (new agents).
FIX: Define an explicit cold-start policy: v1 gates on a mandatory baseline assert-suite (the open question Q3 baseline suite — make it a MUST, not open) rather than a version-diff; require N seed golden cases before the version-diff gate activates; and treat an empty/absent golden set as a HARD FAIL (block), not passRate=1, so a missing suite can never masquerade as a green gate.
VERIFY: Core defect is real but the "deadlock" framing is overstated and partly misreads the PRD. CONFIRMED part: the PRD never specifies FR-EVAL-4 cold-start behavior for a brand-new agent's v1, Q3 (baseline assert-suite) is left OPEN rather than a MUST, and the repo proves an empty suite silently passes — harness.ts:187 (passRate = results.length===0?1:...), :180 (byFailureMode empty→1), and isRegression at :198 (1 < baseline−tol is false), so an absent golden set masquerades as green, exactly R6's vanity-pass and a G2 violation. REFUTED parts that lower severity: (1) the finding's premise that v1 cases can't be obtained misreads FR-EVAL-3, which says the golden set "засевается вручную" (seeded manually) AND grows from prod — manual seeding is available at v1, so there is no structural deadlock. (2) Branch (a) "hard-blocks forever" is unsupported and self-contradictory (a missing baseline yields a false-negative PASS, not a block — confirmed by repo). (3) The real AgenticMind gate (scripts/eval.ts:47,61) uses an ABSOLUTE baseline (BASELINE_PASS_RATE default 0.8), not the version-diff FR-EVAL-4 describes, and ships a 224-case hand-generated seed (build-eval-cases.ts), so the implemented path already gates v1 against a fixed threshold. Net: a genuine under-specification of the gate on empty/first-version suites, not a cold-start deadlock; the empty-suite=1.0 footgun is the actionable, verifiable kernel.
STEELMAN: Even granting manual seeding, the PRD makes FR-EVAL-4 a MUST while leaving the seed's existence and minimum size unenforced (Q3 open), and A2 only requires a mature golden set for L3 — so nothing in the spec prevents a team from wiring the SDK, shipping v1 with zero eval cases, and getting a green CI gate. The repo confirms this is not hypothetical: an empty suite returns passRate 1.0 and passes isRegression against any baseline, which is precisely the false progress R6 warns against and directly breaks G2 ("regressions don't reach prod") on the most common path (new agents). The recommendation to make Q3 a MUST, require N seed cases before the gate activates, and treat an empty/absent golden set as a HARD FAIL is a sound, low-cost hardening the PRD should adopt regardless of the deadlock framing.

====================================================================================================
[MEDIUM] PLAUSIBLE  eval#4  (eval)
FR: FR-EVAL-3, FR-FAIL-1, FR-EVAL-4, R6
TITLE: Selection bias baked into the loop: sampling only failures skews the golden set and blinds the gate to regressions on the happy path
CLAIM: FR-EVAL-3 grows the golden set from an 'еженедельный сэмпл фейлов' (weekly failure sample) and FR-FAIL-1 routes only failing signals into the pipeline, so the golden set drifts toward hard/failed cases and under-represents the passing distribution the gate must protect.
WHY: A gate whose corpus is dominated by past failures measures 'did we fix known bugs', not 'did we regress working behavior'. Failure scenario: agent gets patched to pass the accumulated failure-heavy golden set; because the set contains few nominal cases, a change that quietly breaks a common happy-path query (never a past failure, so never sampled) passes the gate at high pass-rate — a false green. Over time the set also overfits: the L2 improver (FR-IMPROVE-2/L3) tunes prompts against the same cases the gate scores, so the golden set becomes a training set and the score inflates without generalization (classic eval leakage). The existing repo already shows the failure-mode-bucket instinct (harness byFailureMode) but no stratified sampling to counter bias.
FIX: Mandate stratified sampling (successes + escalations + abstentions, not just thumbs-down/failed-assert), cap the fraction of failure-derived cases, and enforce a train/gate split so cases used by the L2/L3 improver are held OUT of the promotion gate's scoring set (the PRD currently reuses one golden set for both improvement and gating). Add a periodic 'holdout freshness' rotation to detect overfitting.
VERIFY: Both sub-claims are real but partly mitigated, so neither CONFIRMED nor REFUTED. (A) Selection bias: FR-EVAL-3's ONLY explicit growth mechanism is the weekly failure sample, and nothing caps the failure fraction or mandates stratified/nominal ingestion — so the golden set CAN drift failure-heavy over time. But the finding overstates "sampling only failures": FR-EVAL-3 seeds the set manually first, FR-EVAL-2c mandates a *rotating* human sample as ground truth (not failures), FR-EVAL-5 (SHOULD) makes 100% of prod traffic available to replenish the eval set, and FR-EVAL-4 gates *relative to the current prod version* so a happy-path regression would surface IF represented. The AgenticMind repo (scripts/build-eval-cases.ts) already seeds nominal buckets (factual_retrieval, citation_grounding, out_of_scope), not just failures, and selectBestParams (packages/shared/src/lib/knowledge/retrieval-params.ts:92) guards per-failure-mode regression — a real counter to the "false green on happy path" scenario. So drift is possible but the PRD is not silent; the gap is the absence of a stratification mandate / failure-fraction cap, which is a legitimate SHOULD-level improvement. (B) Eval leakage: the finding under-credits FR-IMPROVE-3 (L3), which adds an *independent* calibrated judge AND A/B against live prod before promote — A/B on real traffic is exactly the out-of-sample generalization check that catches golden-set overfitting, plus "без новых кластеров" as an OOD guard. BUT L3/A-B is explicitly out of MVP (§2 "A/B... переносится в фазу 5"; §12 Phase 5). For the MVP MUST levels L1/L2 (FR-IMPROVE-1/2) the improver ships through the eval-гейт ONLY, with no holdout and no independent judge — one golden set serves both improvement and gating. The repo confirms this collapse concretely: scripts/tune.ts optimizes candidate profiles against eval/cases.json and then selectBestParams scores/gates against the SAME eval/cases.json — no train/gate split exists. So the leakage risk is genuine at the MVP autonomy tiers. Net: a real design gap the PRD does not fully close, but with multiple existing partial defenses (relative-to-prod gate, rotating human GT, per-mode guard, and A/B+independent-judge at L3) → PLAUSIBLE, downgraded to medium since the worst-case scenario is strongly mitigated at L3 and partially at eval composition.
STEELMAN: The PRD is not naive about eval integrity: it gates *relative to the current prod version* (FR-EVAL-4), so a break in common happy-path behavior manifests as a measurable drop against the incumbent rather than a silent pass — provided the golden set carries nominal cases, which FR-EVAL-3 (manual seed) + FR-EVAL-2c (rotating human sample) + FR-EVAL-5 (100% traffic replenishment) all supply. On leakage, the highest-autonomy tier (L3, FR-IMPROVE-3) already enforces the finding's own recommended defense — an independent calibrated judge plus A/B against live production traffic — which is a true out-of-sample generalization gate that no amount of golden-set overfitting can fake. The lower tiers (L1/L2) keep a human in the loop reviewing patches, so pure automated overfitting to the corpus is bounded by human judgment before ship. The AgenticMind repo demonstrates the team already thinks in per-failure-mode terms and refuses to ship a quiet per-mode regression (selectBestParams), i.e. the anti-vanity-metric instinct R6 targets is operationalized in code. Adding a failure-fraction cap, a stratified-sampling mandate, and an L1/L2 train/gate holdout would harden the design, but they are incremental SHOULD-level tightenings on top of an eval philosophy that is already relative-baselined, human-in-the-loop, and A/B-validated at the point it goes fully autonomous.

====================================================================================================
[MEDIUM] PLAUSIBLE  eval#5  (eval)
FR: FR-EVAL-4, FR-EVAL-2b, FR-JUDGE-2, NFR-OBS-1
TITLE: LLM-in-CI is unbudgeted and nondeterministic — and today it isn't actually run in CI at all
CLAIM: FR-EVAL-4 runs the eval suite (incl. calibrated LLM judge, FR-EVAL-2b) on every agent_version change as a blocking gate, but the repo's own integration eval is explicitly NOT wired into CI (ci.yml:46-48 is a commented note requiring DATABASE_URL+CHAT_API_KEY), and an LLM-based gate introduces cost, latency, and flakiness the PRD never bounds.
WHY: Running real product agents + an LLM judge per PR means: (1) who runs the product agent binary in CI? The PRD's SDK instruments external products — their agent code/creds/tools aren't present in APL's CI. (2) LLM judges are stochastic; harness.ts has a 0.02 tolerance but a binary judge flipping on borderline cases can push pass-rate across the gate nondeterministically → flaky gate that blocks good deploys and erodes trust (teams add --force). (3) Cost: hundreds of golden cases × judge calls × every push is unbounded spend, exactly R5's concern but for CI not prod. Failure scenario: a 200-case suite with a GPT-4-class judge takes 8 min and $6 per run, flakes 3% of the time, and requires the product's live tool creds in the APL CI sandbox — so teams disable it, and FR-EVAL-4 becomes theater.
FIX: Separate the gate tiers by determinism: run Level-1 deterministic asserts as the hard blocking gate in PR CI (cheap, reproducible — this is what ci.yml already does), and run the LLM-judge Level-2/3 suite as an out-of-band pre-promotion gate on a fixed frozen case set with a pinned judge_version, seeded/temperature-0 where possible, majority-vote over k samples to bound flakiness, and an explicit cost/latency budget (NFR-OBS-1 should track 'cost of judge runs' — already listed). Specify where product-agent execution happens (dedicated eval runner with scoped creds), because CI cannot execute arbitrary tenant agents.
VERIFY: Repo facts check out: ci.yml:46-48 is a commented note (unit tests run via `bun test`, but `bun run eval` against the live engine + judge is NOT wired — needs DATABASE_URL+CHAT_API_KEY), harness.ts:197 has the 0.02 tolerance, and JudgeForEval is a bare stochastic binary Promise<boolean> with no seeding/majority-vote/temperature. Two of the finding's gaps are genuinely unaddressed by the PRD: (a) FR-EVAL-4 says the gate runs "on every agent_version change" but never specifies WHERE the external product agent executes or in whose CI — which contradicts the PRD's own model (NG1/FR-SDK: APL instruments products from OUTSIDE, so their code/creds/tools aren't in APL's CI); and (b) no CI cost/latency/determinism budget exists — NFR-OBS-1 only OBSERVES judge cost, R5 bounds prod cost not CI. But the finding overreaches: (1) "isn't run in CI today" judges the current AgenticMind engine CI against a forward-looking Draft v0.1 spec whose Фаза 2 (§12) explicitly delivers this gate later; (2) the flakiness concern is partly mitigated by the shipped 0.02 tolerance band (one flipped case in a 200-case suite ≈0.5% < 2%) plus FR-JUDGE-2/3 calibration gating (TPR/TNR>80%); and (3) the recommended tier-separation already exists as the PRD's three-level pyramid (FR-EVAL-2a/2b/2c). Real but arguable/partly-mitigated, and not correctness- or safety-breaking → PLAUSIBLE, medium.
STEELMAN: The PRD already contains every ingredient the finding asks for: FR-EVAL-2 splits deterministic asserts (2a) from the LLM judge (2b) from human ground-truth (2c) — exactly the deterministic-hard-gate vs out-of-band-judge tiering recommended; FR-JUDGE-1/2/3 make judges versioned and calibration-gated (only TPR/TNR>80% judges may gate, bounding error and enabling a pinned judge_version); the repo already ships a baseline+tolerance regression gate (isRegression, BASELINE_PASS_RATE default 0.8); and NFR-OBS-1 already names "стоимость судейских прогонов" as an observability target. The commented ci.yml line is honest scaffolding for a Draft spec, not a contradiction — Phase 2's gate is scheduled work, and the pyramid means the cheap Level-1 asserts can be the blocking PR gate while the judge tier runs pre-promotion, which is exactly the finding's own recommendation. So the finding is best read as "compose these existing pieces into an explicit CI cost/determinism/execution-locus policy," not as a design that's wrong.

====================================================================================================
[MEDIUM] CONFIRMED  judge#3  (judge)
FR: FR-IMPROVE-3, FR-JUDGE-1, R2
TITLE: 'Independent calibrated judge' in L3 is not independent — one judge prompt exists and shares lineage with the evals it gates
CLAIM: FR-IMPROVE-3 requires an 'independent calibrated judge' to confirm auto-improvements, but there is a single judge prompt (JUDGE_SYSTEM in feedback-judge.ts) reused everywhere, and the PRD never defines what independence means (different model? different prompt author? different label set?).
WHY: When the judge that gates the deploy is the same family/prompt as the judge used inside the eval suite and the promoter (feedback-promoter.ts defaultJudge), a prompt-overfitting or systematic blind spot in that judge is invisible to the gate — the auto-improver can evolve the agent prompt to exploit the judge's weakness (Goodhart), auto-merge to canary, and pass because the 'independent' verifier shares the exploit. This is precisely R2 ('self-improvement toward noise'), which the PRD claims to mitigate but does not, because it leaves independence undefined. Failure scenario: L2 proposes a few-shot patch that adds confident-sounding boilerplate the judge rewards; judge score rises, real quality drops, gate opens.
FIX: Define independence operationally in FR-IMPROVE-3: the L3 gating judge MUST differ from the calibration/eval judge in at least model provider AND prompt authorship, and MUST be calibrated on a disjoint held-out label set (see finding above). Add a required check that the gating judge is re-calibrated and passes on data the patch was not fit to. Encode the 'oracle distinct from judge-under-test' rule from eval/README.md as an FR.
VERIFY: The core claim holds against both the PRD text and the repo. PRD line 147 (FR-IMPROVE-3) literally requires "независимый откалиброванный судья" (independent calibrated judge) but nowhere defines independence — no FR distinguishes the L3 gating judge from the eval-suite judge (FR-EVAL-2b) along model, prompt-authorship, or label-set axes. The judge data model (line 192) is a single entity {prompt, calibration, version}; there is no second/oracle judge in the spec. R2 (line 249) claims to prohibit "self-improvement toward noise" citing FR-JUDGE-2 + FR-IMPROVE-3, but calibration on a fixed label set does not prevent Goodhart/blind-spot exploitation when the improver evolves the agent prompt against the same judge — so R2 overclaims its mitigation. Repo confirms the feasibility premise: a single JUDGE_SYSTEM prompt (feedback-judge.ts:11) is reused by the promoter's defaultJudge (feedback-promoter.ts:117) AND by scripts/calibrate.ts:26 — one prompt everywhere. Most damning for the PRD: the repo's own eval/README.md:41 already states the exact remedy the finding recommends ("keep the oracle distinct from the judge under test to avoid circularity"), yet the PRD failed to promote that rule to a requirement. Adjusted high→medium because L3 is a SHOULD, opt-in per agent (FR-IMPROVE-6), maturity-gated (A2), blast-radius-bounded to canary+A/B before promote, and hard-limited by FR-IMPROVE-4/NFR-SEC-1 to prompt/context/few-shot patches only — so the worst case is silent quality drift, not a trust/security breach. It is a real under-specification gap in the flagship claim, not a ship-blocker.</reasoning>
<parameter name="steelman">The PRD is not defenseless. (1) FR-EVAL-2(c) mandates a "rotating human sample as ground truth" and FR-EVAL-3 mines production failures into the golden set — a human channel independent of any LLM judge exists, so systematic judge blind spots surface over time. (2) FR-IMPROVE-3 does not gate on the judge alone: it requires full eval + no regression on golden set + no new failure clusters (tied to FR-FAIL-3 trend alerts) + auto-merge only to canary + A/B against prod before promote — a Goodhart patch that raises judge score but drops real quality would plausibly trip the golden-set-regression or new-cluster gate, or lose the A/B. (3) FR-IMPROVE-4 hard-caps auto-patches to prompt/context/few-shot, so even a fully exploited judge cannot touch tools, permissions, or trust boundaries. (4) The PRD is intentionally a requirements doc, not an implementation; "independent" being left to the eng-level open questions (Q1) is arguably appropriate deferral, and the eval/README.md oracle-distinctness convention shows the team already holds the principle operationally. On this reading the residual risk is a bounded, catchable, opt-in quality drift, not the invisible auto-deploy-of-exploit the finding paints.

====================================================================================================
[MEDIUM] CONFIRMED  judge#4  (judge)
FR: FR-JUDGE-1, FR-JUDGE-3, FR-EVAL-4
TITLE: Judge drift across model upgrades has no re-calibration trigger; calibration is treated as a one-time gate
CLAIM: FR-JUDGE-3 re-runs calibration only 'when the judge PROMPT changes', but the dominant drift source — the underlying LLM being silently upgraded by the provider (gpt-4o -> a new snapshot) — leaves the prompt hash unchanged and therefore never re-triggers calibration.
WHY: calibrate.ts pins model: 'gpt-4o', a moving alias. A provider-side model refresh changes verdict behavior with an identical prompt hash; under FR-JUDGE-3 the stored TPR/TNR stays 'valid' while the live judge has drifted, and CI keeps gating deploys (FR-EVAL-4) with a stale-calibrated judge. Result: regressions pass the eval gate for weeks because the certificate that the judge is trustworthy is stale. The judge-as-versioned-entity (FR-JUDGE-1) keys on prompt only, so model identity is not even part of the version.
FIX: Include the exact model snapshot id (not the alias) and the semconv/judge-config version in the judge_version hash, and expire calibration on any of {prompt change, model-id change, calibration age > N days}. Require pinned snapshots (e.g. gpt-4o-2024-11-20) rather than floating aliases in calibrate.ts. Block gating on stale calibration the same way calibrate.ts already process.exit(1)s on uncalibrated.
VERIFY: The defect holds against both PRD text and repo. FR-JUDGE-3 (PRD line 130) re-runs calibration ONLY "при изменении промпта судьи" — prompt change is the sole trigger; no model-id or time-based trigger. FR-JUDGE-1 (line 128) and the judge data model (line 192: `prompt, calibration(human_labels, TPR, TNR), version`) version the judge on prompt + calibration only — model is NOT a field of the judge entity. Repo confirms the footgun: scripts/calibrate.ts:28 pins `model: "gpt-4o"`, a floating alias; a repo-wide grep found zero dated snapshot ids anywhere. judge-calibration.ts's CalibrationReport has no model field, no timestamp, and no age/expiry — pure confusion matrix, so a provider refresh under an unchanged alias leaves TPR/TNR "valid" while the live judge has drifted, and FR-EVAL-4 keeps gating with it. I searched the whole PRD for drift/snapshot/expiry/provider/stale mitigations and found none in the FR sections or risk list (R1–R7); the closest, FR-INTEG-3, treats model only as a captured telemetry attribute, not a calibration-invalidation trigger — exactly the gap. Adjusted high→medium: the finding overstates blast radius ("regressions pass for weeks") since the eval pyramid's other tiers — deterministic asserts (FR-EVAL-2a) and the rotating human ground-truth sample (FR-EVAL-2c) — act as independent backstops that would surface a badly drifted judge, and FR-JUDGE-3's "publish TPR/TNR every release" gives indirect recalibration at release cadence. Real, unmitigated spec gap, but partially contained.
STEELMAN: The PRD could argue this is a v1 scoping choice, not a blind spot: model IS a first-class attribute elsewhere (FR-REG-2 hashes it into agent_version, FR-CONTRACT-3 captures gen_ai.request.model per trace, FR-INTEG-3 makes provider/model explicit), and FR-JUDGE-3 mandates publishing TPR/TNR "с каждым релизом" — so a disciplined release cadence re-calibrates regularly, and the rotating human ground-truth tier (FR-EVAL-2c) plus deterministic asserts (FR-EVAL-2a) form a defense-in-depth net that catches a drifted judge even between recalibrations. Under that reading, "no explicit model-drift trigger" is a hardening detail (pin snapshots, add calibration TTL, add judge model to the version key) that fits Phase 5 (L3 judge-gated auto), not a MUST-level hole in the current gate.

====================================================================================================
[MEDIUM] unverified  contract-otel#4  (contract-otel)
FR: FR-CONTRACT-5, NFR-TENANT-1
TITLE: moai.* per-span namespacing conflates identity (tenant/product) with span data — tenant_id/product_id belong on Resource, not every span
CLAIM: FR-CONTRACT-5 puts tenant_id, product_id, agent_id, agent_version as `moai.*` span attributes on (implicitly) every span. Stable service/tenant identity is Resource-level in OTel, not span-level; only per-invocation facts (task_id, outcome, human_feedback, decision_reason) are naturally span attributes. The repo already models this split correctly: service identity via resourceFromAttributes (tracing.ts) vs per-phase data via span attrs (trace.ts).
WHY: Stamping tenant_id/product_id on every child span (chat, execute_tool, N of them per request) is redundant storage on the append-heavy ClickHouse path (FR-INGEST-3) and, more dangerously, makes tenant isolation a per-span-attribute invariant rather than a resource/stream invariant — one span that forgets the attribute becomes cross-tenant-ambiguous data at rest, directly weakening NFR-TENANT-1's 'cross-tenant leak impossible at storage layer.' Isolation enforced by hoping every span carries the right attribute is fragile.
FIX: Put tenant_id/product_id (and stable agent_id/agent_version identity) on the OTel Resource (or a single root-span baggage/attribute propagated once), and reserve moai.* span attributes for per-invocation facts (task_id, outcome, human_feedback, decision_reason). Enforce tenant isolation at the ingestion boundary keyed on resource, not by trusting a span attribute. Keep the vendor prefix — that part is right and matches the existing 'agenticmind.*' pattern — but 'moai.' fragments the org's own namespace from the shipped 'agenticmind.' prefix; pick one.

====================================================================================================
[MEDIUM] unverified  contract-otel#5  (contract-otel)
FR: FR-CONTRACT-6, A1
TITLE: FR-CONTRACT-6 calls async sub-agent context propagation 'the main multi-agent failure mode' — but AgenticMind has no multi-agent/async-fan-out surface to ground it
CLAIM: FR-CONTRACT-6 (and R7) frame broken span lineage across async sub-agent boundaries as the primary multi-agent observability failure. The AgenticMind reality is a single-process RAG pipeline (server: retrieve→synth→filter, one root span 'knowledge.ask') plus one daily Postgres-scheduled worker sweep that opens its OWN unparented root span (handler.ts:24). There is no sub-agent fan-out, no queue/broker (index.ts: 'no broker … no separate queue'), and zero use of the OTel propagation/context API anywhere.
WHY: The requirement is real for the *target* products APL will instrument, but as written it (a) has no in-repo analogue to reuse or test against, and (b) understates the concrete boundary that DOES exist and DOES drop lineage today: the worker sweep. If APL's own self-improvement loop (worker) is meant to be traced end-to-end with the request that triggered it, it currently can't be — the worker roots a fresh trace with no link back. Shipping FR-CONTRACT-6 as 'sub-agent async propagation' misses that the first propagation gap to fix is server→worker (which spans a process boundary + Postgres schedule, harder than in-process async).
FIX: Split FR-CONTRACT-6 into two: (1) in-process async propagation (Promise.all fan-out, awaits) — solvable with OTel context.with()/active-span, and add a fixture multi-agent product to test the acceptance criterion since none exists in-repo; (2) cross-process/deferred propagation (server→queue→worker) using injected traceparent + span links, which is the actually-hard case this codebase already exhibits. Do not assert 'main failure mode' without a reproduction; add a concrete failing-trace fixture to Phase-0 acceptance.

====================================================================================================
[MEDIUM] unverified  contract-otel#6  (contract-otel)
FR: FR-CONTRACT-2, FR-CONTRACT-3
TITLE: Acceptance criterion 'identical tree structure from LangGraph and raw-code products' is unachievable as specified and untestable in-repo
CLAIM: The §6.1 acceptance criterion requires a trace from a LangGraph product and a raw-code product to yield an IDENTICAL tree structure and identical mandatory gen_ai.*+moai.* attribute sets. Framework instrumentors differ in span granularity (LangGraph emits chain/graph-node spans; a raw loop emits only invoke_agent→chat/execute_tool), and — per finding above — differ in span naming (operation+model suffix vs bare).
WHY: 'Identical' is the wrong bar: two conformant emitters routinely produce structurally different but semantically-equivalent trees (extra framework-internal spans, different names). Setting 'identical' as the DoD guarantees the gate can't pass without either lobotomizing the LangGraph adapter (dropping its native spans) or declaring victory loosely. There is also no reference product in the AgenticMind repo to run this against — it's asserted, not exercised.
FIX: Restate the criterion in terms of a canonical *projection*: after APL's mapping layer, both traces MUST expose the same set of {invoke_agent, chat, execute_tool} operation spans with the mandatory attributes present and correctly parented — extra framework spans are allowed and preserved. Define equivalence on operation.name + parentage + attribute presence, not byte-identical trees. Add two reference fixture products to the repo so the gate is actually runnable.

====================================================================================================
[MEDIUM] unverified  sdk#5  (sdk)
FR: FR-SDK-8, NFR-PERF-1, G1
TITLE: '<1% overhead' (NFR-PERF-1/FR-SDK-8) is unfalsifiable as written — no baseline, workload, or metric defined
CLAIM: The <1% overhead budget has no definition of the baseline, the workload, what 'main thread' means for an I/O-bound async agent, or how it's measured; the repo has no benchmark harness (grep overhead/benchmark = build artifacts only).
WHY: Agent execution is dominated by LLM network latency (seconds); span creation is microseconds, so '<1% of end-to-end latency' is trivially true and meaningless, while '<1% of CPU on a tight synchronous loop' could be violated by attribute serialization of large gen_ai.input.messages (FR-CONTRACT-7) — the two readings give opposite verdicts. Failure scenario: content-capture on a high-QPS product serializes 2KB-capped strings per span (trace.ts cap=2000) thousands of times/sec, GC pressure spikes, and the team argues forever about whether the budget was met because the metric was never pinned.
FIX: Define NFR-PERF-1 concretely: metric (added CPU-ms per agent turn and added p99 latency), baseline (SDK disabled), a reference workload, and content-capture ON vs OFF variants. Provide the benchmark as an acceptance artifact, not a prose claim.

====================================================================================================
[MEDIUM] unverified  sdk#6  (sdk)
FR: G1, NFR-DX-1, FR-SDK-1, FR-INTEG-2
TITLE: G1/NFR-DX-1 '≤1 day time-to-first-trace' rests on net-new load-bearing components and is not supportable from current repo state
CLAIM: The ≤1-day integration promise depends on wrapAgent (FR-SDK-1), auto tool instrumentation (FR-SDK-2), and framework adapters (FR-INTEG-2) — all net-new (grep = 0), plus a running Collector and (per PRD) ClickHouse. Today, instrumenting even AgenticMind itself meant hand-placing spans.
WHY: The claim conflates 'call one function' with the actual first-trace path: install SDK + wrap loop + configure OTLP endpoint + stand up Collector (+ redaction processor + ClickHouse for the trace store per FR-INGEST-3) + choose sampling. For a self-hosted/VPC tenant (FR-INTEG-4) the infra alone exceeds a day. Failure scenario: a product engineer wraps the loop in an hour but spends three days getting the Collector + ClickHouse + redaction chain up before a single trace is queryable, so G1 fails on the exact enterprise path §1 monetizes.
FIX: Scope G1 to the SDK-emit milestone against a HOSTED endpoint (SaaS) explicitly, and give self-hosted a separate, honest onboarding SLA that includes the collector/store bring-up. Ship a one-command dev collector to make the hosted path credibly ≤1 day.

====================================================================================================
[MEDIUM] unverified  ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3)#5  (ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3))
FR: FR-REG-2, FR-REG-4, §8 agent_version
TITLE: Immutable agent_version stores full prompt AND prompt_hash with no dedup/canonicalization strategy
CLAIM: FR-REG-2/§8 store both prompt (full text) and prompt_hash in every immutable version, plus tools[], params, context_strategy, git_ref. There is no de-duplication (identical prompts across versions stored verbatim), no defined hash canonicalization (whitespace/ordering), and no rule for what counts as a 'config change' that mints a new version (FR-REG-4).
WHY: Two concrete problems. (1) Churn/bloat: an agent re-deployed dozens of times/week with tiny param tweaks stores the full multi-KB prompt each time; combined with per-trace attribution the version table grows and every scorecard join slows. (2) Ambiguous change detection: if tools[]/params are stored as unordered JSON, [toolA,toolB] vs [toolB,toolA] or {temp:0.7} vs {temperature:0.70} either spuriously mint new versions or collide under the hash, breaking FR-REG-4 and the regression-attribution acceptance criterion.
FIX: Define prompt_hash as sha256 over a canonicalized (trimmed, normalized) prompt and reference prompts by hash (store each distinct prompt once; version rows point to it). Specify a deterministic canonical serialization for tools[]/params/context_strategy that the hash is computed over, and state precisely which fields participate in version identity. Enforce immutability with REVOKE UPDATE / a trigger, mirroring the append-only discipline in the ask-clusters state machine.

====================================================================================================
[MEDIUM] unverified  ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3)#6  (ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3))
FR: FR-INGEST-5, FR-FAIL-2, FR-INGEST-3
TITLE: FR-INGEST-5 pgvector search over fail-traces assumes Postgres, but traces live in ClickHouse and embedding dimension/model are unspecified
CLAIM: FR-INGEST-5 requires pgvector semantic search over fail-traces and FR-FAIL-2 embeds fail-traces for clustering, yet FR-INGEST-3 stores the traces in ClickHouse. The PRD never specifies where fail-trace embeddings live, at what dimension, or how they move from ClickHouse into a pgvector diskann index. AgenticMind is hard-wired to EMBEDDING_DIMENSIONS=1024 (_config.ts:11) as the single source of truth for every vector() column and diskann index.
WHY: Failure scenario: to satisfy FR-INGEST-5/FR-FAIL-2 you must ETL selected fail-traces from ClickHouse back into a Postgres embeddings table — an unspecified pipeline with its own tenant-scoping, freshness lag, and cost. If APL also picks a different embedding dimension than 1024, it cannot reuse the existing diskann/vector_cosine_ops conventions (chunks.ts:50, ask-clusters.ts:50) and fragments the vector layer.
FIX: Resolve the store contradiction first: if traces live in Postgres, fail-trace embeddings sit next to them and FR-INGEST-5 is a direct reuse of the chunks/ask-clusters diskann pattern. Pin the embedding model+dimension to EMBEDDING_DIMENSIONS (1024) or explicitly justify a separate vector space. If the ClickHouse split survives, specify the fail-trace->Postgres embedding ETL as a first-class FR with retention and tenant-scoping.

====================================================================================================
[MEDIUM] unverified  ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3)#7  (ingest-store-registry (FR-INGEST-1..5, FR-REG-1..4, §8 data model, C3))
FR: FR-INGEST-4, NFR-PRIV-1, FR-SDK-7, §8 trace/span
TITLE: Trace content storage regresses from the engine's hash-only privacy pattern; retention/PII rules underspecified for the trace store
CLAIM: The closest analogue ask-telemetry.ts deliberately stores only questionHash (sha256), never question text ('Privacy: only the sha256 ... is stored, never the question text'). APL's trace store instead captures gen_ai.input/output.messages content (FR-CONTRACT-7, FR-SDK-7 why-trace) subject only to best-effort emit-time redaction (FR-SDK-5). §8/FR-INGEST-4 give retention as a loose 30-90d range with no per-tenant/per-severity policy and no storage-layer interaction with NFR-PRIV-1.
WHY: This regresses from the engine's own hash-only posture into storing raw LLM I/O. Failure scenario: emit-time redaction misses a PII pattern, the raw prompt/output lands in the trace store, and with retention set to 90 days that PII sits for a quarter in a store that (under FR-INGEST-3) isn't even covered by Postgres RLS. Open question Q2 (retention per mode/tenant) is acknowledged but unresolved while FR-INGEST-4 is marked MUST.
FIX: Make content-capture opt-in AND default-off at the storage layer (mirror ask-telemetry's hash-first stance), define retention as an enforced per-tenant policy (TimescaleDB add_retention_policy if traces are in Postgres) rather than a range, and specify a storage-layer PII purge/redaction fallback so NFR-PRIV-1 does not rest solely on best-effort emit-time redaction. Resolve Q2 before FR-INGEST-4 can be MVP-MUST.

====================================================================================================
[MEDIUM] unverified  eval#6  (eval)
FR: FR-JUDGE-2, FR-JUDGE-4, FR-EVAL-2c
TITLE: Judge calibration premise (≥100 human labels) is unmet and the repo deliberately substitutes synthetic/agent oracles — creating a circularity risk
CLAIM: FR-JUDGE-2 requires calibration on ≥100 HUMAN labels with TPR/TNR>80%, and FR-JUDGE-4 reuses the FR-EVAL-2c human annotations, but the existing calibration set is 110/129 synthetically constructed (build-eval-cases.ts) and README explicitly makes the top-tier oracle a stronger MODEL, not humans.
WHY: The repo's judge-labels.json is 'constructed obvious-grounded/obvious-ungrounded pairs' whose label is guaranteed by construction (build-eval-cases.ts:329-339). Calibrating a judge on trivially-separable synthetic pairs yields a TPR/TNR near 100% that says nothing about the judge's accuracy on real borderline prod cases — the exact cases the gate must adjudicate. Failure scenario: judge reports 'calibrated, TPR/TNR=1.0' off synthetic labels, is admitted to gate deploys (FR-JUDGE criteria), then in prod mislabels ambiguous agent outputs, so a regressed version is promoted with a green judge stamp. Reusing FR-EVAL-2c annotations (FR-JUDGE-4) is doubly unsound if 2c is itself an agent pass (README), because the judge is then calibrated against an oracle of the same family it's supposed to check — circularity the README warns about but the PRD reintroduces.
FIX: Make the human-label requirement real and auditable: require the calibration set to be genuinely human-labeled AND drawn from the borderline/prod distribution (not synthetic separable pairs), report the human/synthetic split, and forbid calibrating a judge against an oracle in its own model family (keep the FR-EVAL-2c oracle distinct from the judge under test, as eval/README.md already stipulates). If humans are truly out of scope (matching the repo's stance), rewrite FR-EVAL-2c/FR-JUDGE-2 to say 'independent stronger-model oracle' honestly rather than 'human'.

====================================================================================================
[MEDIUM] unverified  eval#7  (eval)
FR: FR-EVAL-1, FR-EVAL-3, FR-SCORE-1, G2
TITLE: Golden-set drift/leakage as it grows: no versioning of the case set means score trends (G2/FR-SCORE-1) are not comparable over time
CLAIM: The golden set grows continuously from prod (FR-EVAL-3) while G2/FR-SCORE-1 plot a 'quality trend over time', but a score against a moving case set is not a trend — adding hard mined cases mechanically lowers pass-rate even when the agent didn't regress.
WHY: The existing cases.json has no case_set version/hash and eval.ts compares to a flat constant; there is no notion of 'score on frozen set S_t'. Failure scenario: week 1 the agent scores 92% on 100 cases; week 4 the loop added 40 hard failure-derived cases, agent now scores 84% on 140 cases. The scorecard renders a downward 'regression' trend and the gate may block, but the agent's behavior is unchanged — the corpus got harder. Conversely, dropping/curating flaky cases can manufacture a fake uptrend. This makes G2's version-attributed trend (and R4's attribution goal) unreliable.
FIX: Version and hash every case-set revision; store eval_run against a specific case_set_hash; render trends per frozen case-set generation (and re-run the old set alongside the new so old-vs-new are comparable). Gate on the frozen set that the prior version was scored on, then separately report the delta introduced by newly-added cases. This also localizes leakage: mark which cases were consumed by the improver so they can be excluded from the trend/gate.

====================================================================================================
[MEDIUM] unverified  judge#5  (judge)
FR: FR-JUDGE-2, FR-EVAL-2
TITLE: Judge returns a 4-way verdict collapsed to binary by 'supported'-only; PRD's binary TPR/TNR silently discards the partial-support failure mode
CLAIM: The existing judge emits {supported, partially_supported, unsupported, unknown} and judgeAllowsPromotion() maps only 'supported' -> pass; the PRD models calibration as pure binary TPR/TNR and never says how partial_support/unknown are bucketed, which changes what TPR/TNR even measure.
WHY: How 'partially_supported' and 'unknown' collapse to the binary label is a load-bearing methodology choice: treating partial as pass inflates TPR and hides ungrounded content; treating it as fail inflates false negatives and blocks good answers. The current code folds everything non-'supported' into fail (feedback-judge.ts line 71), which is a conservative but undocumented choice. If the PRD's calibration harness reuses a different collapse (e.g. partial=pass), the same judge yields different TPR/TNR, and the >80% gate becomes non-comparable across teams/agents — undermining the cross-agent scorecard (FR-SCORE) and CI gate (FR-EVAL-4).
FIX: Specify the verdict->binary mapping in FR-JUDGE-2 as a fixed contract (recommend: only 'supported' counts as positive, matching judgeAllowsPromotion), and require the mapping to be stored with the judge_version so TPR/TNR are reproducible. If partial-support matters for a given agent, define it as a separate labeled class rather than silently folding it.

====================================================================================================
[MEDIUM] unverified  judge#6  (judge)
FR: FR-JUDGE-3, FR-JUDGE-1
TITLE: Judge-prompt overfitting to its own calibration set is unbounded — no held-out judge test set
CLAIM: FR-JUDGE-3 publishes TPR/TNR from the same label set used to iterate the judge prompt (misses are the 'prompt-tuning fuel' per judge-calibration.ts docstring), so the reported >80% is an in-sample training score, not a generalization estimate.
WHY: The calibration report explicitly surfaces misses to tune the prompt against; iterating the JUDGE_SYSTEM prompt until misses drop is fitting to the calibration set. Publishing that same TPR/TNR as the trust certificate (FR-JUDGE-3) overstates real accuracy — the judge looks calibrated on the 129 labels it was tuned on and degrades on novel production failures, which is exactly where L2/L3 rely on it. Because there is one label file (eval/judge-labels.json), there is no untouched split to detect this.
FIX: Mandate a train/test split of judge labels: tune the judge prompt only on a dev split, publish TPR/TNR on a locked test split never inspected during prompt iteration. Grow labels with this split budget in mind (the >=50-per-class stratification from the first finding should apply to the test split specifically).

====================================================================================================
[MEDIUM] unverified  failure#6  (failure)
FR: FR-FAIL-4, FR-INGEST-3
TITLE: Per-cluster cost/latency (FR-FAIL-4) is impossible on the current data model and its mandated store contradicts a stated non-goal
CLAIM: FR-FAIL-4 wants cost/latency per agent/version/cluster, but ask_telemetry latency is keyed by servedBy path and questionHash with no agent, agent_version, or cluster join, and the ClickHouse store FR-INGEST-3 routes this to is an explicit AgenticMind non-goal.
WHY: ask-telemetry.ts has retrievalMs/generationMs but no cluster_id/agent_version columns, and ask_cluster_members joins asks to clusters only for the knowledge-promotion flow, never carrying cost. There is no agent_version table to attribute cost to a version. ROADMAP.md:36 ('stays Postgres-only on purpose') and CONTRIBUTING.md:22-23 (no hard ClickHouse dependency) directly oppose C3/FR-INGEST-3. Failure scenario: an operator asks 'which failure cluster is burning the most tokens per week on agent X v3?' — answerable only by a three-way join (trace-cost × cluster-membership × version) across a store the host engine forbids adding as a hard dependency.
FIX: Either (a) keep it Postgres-only and scope FR-FAIL-4 to aggregates computable from a per-failure cost column on the (net-new) failure table, dropping the ClickHouse mandate to SHOULD/optional behind the CONTRIBUTING.md interface seam; or (b) explicitly flag C3 as overriding an AgenticMind non-goal and get sign-off, because as written the two documents contradict.

====================================================================================================
[MEDIUM] unverified  failure#7  (failure)
FR: FR-FAIL-2, G3, §8 failure_cluster
TITLE: Existing clusters are tenant-scoped but NOT agent-scoped; G3/FR-FAIL-2 'per-agent categories' has no place to live
CLAIM: The reuse candidate ask_clusters has tenantColumn but no agent_id, so 'stable named categories PER AGENT' (FR-FAIL-2) and G3's per-agent ≤10 budget cannot be expressed by adapting the table as-is without a schema change plus a rework of every cluster query.
WHY: schema/knowledge/ask-clusters.ts spreads ...tenantColumn but has no agent dimension; findNearestCluster/listClustersByState (ask-clusters query) scan clusters globally within a tenant. Failure scenario: two different agents in one tenant with overlapping error phrasings would have their fail-traces merged into one cross-agent cluster (cosine≥0.85 ignores which agent produced it), defeating FR-EVAL-1's whole 'per-agent' philosophy that R1 elevates to a hard prohibition. The 0.85 nearest-neighbour search must be partitioned by agent_id, changing the diskann index usage pattern.
FIX: State that the failure_cluster table is a NEW table (not the ask_clusters table renamed), keyed by (tenant_id, agent_id), with the pgvector search filtered to the agent partition. Confirm the diskann cosine index still performs with an agent_id pre-filter, or plan a per-agent partial-index strategy; otherwise cross-agent contamination silently violates R1/FR-EVAL-1.

====================================================================================================
[MEDIUM] unverified  improve-trust#6  (improve-trust)
FR: A2, FR-EVAL-1, FR-JUDGE-2, FR-IMPROVE-3, FR-IMPROVE-6
TITLE: L3's per-agent preconditions (mature golden set + calibrated judge) are net-new but treated as available assumptions
CLAIM: A2 assumes each L3 agent already has 'a mature golden set and a calibrated judge'. But FR-EVAL-1 mandates a SEPARATE golden set per agent (net-new; the repo has one shared corpus eval/cases.json), and FR-JUDGE-2 requires >=100 human labels with TPR&TNR>80% per judge (the repo has one shared labeled set eval/judge-labels.json). So A2 assumes the completion of two of the hardest, most human-labor-intensive deliverables in the whole PRD.
WHY: 'Opt-in per agent' (FR-IMPROVE-6) will in practice be opt-in for approximately zero agents at MVP, because the gate (100+ human labels per agent + a per-agent golden set that covers the safety-critical failure modes) is unmet for any real fleet on day one. Teams under pressure will either (a) reuse a shared/thin golden set to unblock L3 — reintroducing exactly R1 (global suite) and enabling the reward-hacking above — or (b) L3 ships as dead capability. Either way the G4 goal ('managed cycle with three autonomy levels ... within hard safety bounds') is not actually achieved.
FIX: Make the L3 opt-in gate a code-enforced eligibility check: an agent is L3-eligible only when it has (i) a per-agent golden set of >= N cases spanning its named failure clusters and (ii) a per-agent judge with fresh calibration >= threshold. Surface eligibility on the scorecard. Reframe A2 from assumption to acceptance criterion, and set realistic expectations in §1.1/§12 that L3 lights up gradually as agents earn eligibility, not at MVP GA.

====================================================================================================
[MEDIUM] unverified  integ-nfr-score#6  (integ-nfr-score)
FR: NFR-REL-1, FR-IMPROVE-3, G4
TITLE: Durable execution for L3 (eval→judge→canary→A/B) has no engine; the only durable primitive is a daily advisory-lock sweep
CLAIM: NFR-REL-1 requires pipelines to survive restart without state loss and 'durable execution for long eval/improvement tasks', and FR-IMPROVE-3 defines an L3 flow (propose patch → full eval → independent judge → auto-merge to canary → A/B vs prod → promote or roll back). AgenticMind's durability is explicitly 'Postgres-only, no broker' with a SINGLE daily job scheduled by a Postgres advisory lock (apps/worker/src/index.ts, jobs/knowledge-feedback).
WHY: An advisory-lock daily sweep is a cron, not a resumable multi-step workflow engine. L3 is a long-running, multi-stage, human/judge-gated state machine with canary and A/B stages that must survive worker restarts mid-flight and resume at the right step. Concretely: the worker restarts after 'auto-merge to canary' but before 'A/B vs prod' — with the current pattern there is no persisted workflow state to resume from, so the improvement is left half-applied (a canary running with no A/B decision), which is exactly the kind of unsafe partial auto-change FR-IMPROVE-4/Cycle-of-Trust is meant to prevent. There is also no canary/A/B/rollout machinery in the repo at all (grep confirms the 'promote' hits are knowledge-card promotion, not agent-version rollout).
FIX: Treat L3 orchestration as net-new durable-workflow work with explicit persisted state per improvement (status transitions in the improvement entity as a real state machine) and idempotent, resumable steps. Either model the workflow as rows advanced by an idempotent worker (extending the Postgres pattern honestly) or adopt a workflow engine — and say which. Keep L3 out of MVP (it's already phase 5) and gate it on the durable-state design plus a proven canary/A/B mechanism, neither of which exists today.

====================================================================================================
[MEDIUM] unverified  integ-nfr-score#7  (integ-nfr-score)
FR: G5, FR-INTEG-4, FR-INGEST-1, FR-SCORE-3, FR-SDK-4
TITLE: 'Same contract' SaaS vs VPC ignores the missing OTLP Collector, sampling, and RLS-bypass fleet-read path — G5 is under-specified
CLAIM: G5/FR-INTEG-4 promise one layer with an identical contract across SaaS and self-hosted/VPC, and FR-INGEST-1 requires OTLP ingestion 'compatible with OTel Collector'. But the deploy stack (deploy/docker-compose.yml, setup.sh) stands up only Postgres — no OTel Collector, no ClickHouse, no tail-sampling config (FR-SDK-4). Separately, the fleet view (FR-SCORE-3) requires reading across all tenants, which the FORCE ROW LEVEL SECURITY model (drizzle/0003) actively forbids.
WHY: 'Identical contract' hides three unbuilt operational components that a VPC customer must run and configure themselves: an OTLP Collector (for ingestion + PII redaction + tail-sampling), ClickHouse, and retention policy. A VPC operator following today's deploy gets none of these, so parity with SaaS is fictional until they're specified and packaged. Additionally, the cross-tenant fleet ranking needs a privileged read path that deliberately bypasses RLS (a superuser/BYPASSRLS role or a separate aggregate store) — that path does not exist and is a security-sensitive addition; without it FR-SCORE-3 is unimplementable, and with it there's a new cross-tenant leak surface to secure.
FIX: Specify the full VPC reference topology (Collector config incl. redaction+tail-sampling processors, ClickHouse or Postgres trace store, retention job) and ship it in deploy/ so 'same contract' is real, or scope VPC MVP to a reduced footprint and state the delta from SaaS. For FR-SCORE-3, define the cross-tenant read model explicitly (a dedicated BYPASSRLS aggregation role writing a tenant-scrubbed fleet-metrics table, never raw cross-tenant span access) and add isolation tests for it before building the fleet view.

====================================================================================================
[LOW] PLAUSIBLE  sdk#1  (sdk)
FR: FR-SDK-2, FR-CONTRACT-2, G1
TITLE: 'Auto-instrument execute_tool without per-tool code' has no general mechanism in TS and is overstated
CLAIM: FR-SDK-2 mandates automatic execute_tool span emission 'without per-tool code' across arbitrary product tool abstractions, but AgenticMind's own tools are hand-traced (only ask.ts + one worker handler open spans; mcp-tools.ts handlers are NOT span-wrapped), and TypeScript has no runtime monkey-patch surface for arbitrary user tool functions the way OTel Python auto-instrumentation patches known libraries.
WHY: OTel's tool auto-instrumentation only works where there is a KNOWN framework seam to patch (e.g. an OpenAI Agents SDK / LangGraph tool-invocation hook). For a 'framework-agnostic, not tied to any framework' SDK (NG2), a raw/manual product defines tools as ordinary functions with no common interface — nothing to auto-wrap. Failure scenario: a Moai product wraps its loop with wrapAgent, sees invoke_agent+chat spans, but every tool call is invisible because the SDK cannot discover an arbitrary `async function searchCrm()`; the acceptance criterion 'traces appear without instrumenting each tool' silently fails for exactly the raw-code product the PRD promises parity for (FR-CONTRACT acceptance: 'LangGraph AND bare code give identical trees').
FIX: Split FR-SDK-2 by integration mode: (a) framework adapters (FR-INTEG-2) get auto tool spans via each framework's tool hook; (b) raw/manual mode requires an explicit, thin `instrumentTools(toolMap)` / per-tool wrapTool() — document this as the cost of framework-agnosticism, not 'zero code'. Drop the absolute 'without per-tool code' wording for the raw path.
VERIFY: Factual claims verified in repo: spans are hand-placed only in packages/shared/src/lib/knowledge/ask.ts (knowledge.ask + child retrieve/synthesize) and apps/worker/src/jobs/knowledge-feedback/handler.ts; mcp-tools.ts handlers are NOT span-wrapped; no @opentelemetry/instrumentation-* auto-patch libs are installed (only @opentelemetry/api + sdk-trace). BUT the finding's central inference is refuted by the same repo. It claims raw code has "no common interface — nothing to auto-wrap," yet all 10 knowledge tools route through ONE seam: apps/server/src/mcp.ts:118 registerKlTool → the sole server.registerTool call at line 125. Wrapping that single helper's handle() with withSpan("execute_tool",…) auto-instruments every tool with zero per-tool code — exactly FR-SDK-2. The absence today is that current AgenticMind is an MCP server, not yet APL; it is not a feasibility ceiling. The "TS has no monkey-patch surface like OTel Python" point misreads the PRD's mechanism: §9 defines the SDK API as wrapAgent + "автоинструментация инструментов" (a tool-registration surface, standard OpenLLMetry/OpenInference style), NOT global patching of free-floating functions. FR-SDK-2 says "без ручного кода на каждый инструмент" (per-tool, not per-product) and FR-INTEG-2 already names raw/manual as its own adapter — so the recommended "split by integration mode" is largely already in the PRD. The acceptance criteria promise identical tree STRUCTURE (§6.1) and "traces without instrumenting each tool" (§6.2), neither of which asserts zero SDK integration for raw mode. Residual truth: for a pure-raw product that never routes tools through any SDK surface, "zero code" is impossible in JS, so the absolute wording "без ручного кода" could be tightened — a low doc-precision nit, not a critical contradiction or infeasibility.
STEELMAN: The PRD's strongest defense: it never claimed OTel-Python global auto-patching. Its model is an SDK-provided tool-registration/wrap surface (wrapAgent + автоинструментация инструментов in §9) exactly analogous to how AgenticMind already funnels all tools through one registerKlTool helper — dropping a single withSpan there yields execute_tool spans for every tool with no per-tool code, satisfying FR-SDK-2 literally and framework-agnostically. FR-INTEG-2 already treats raw/manual as an explicit adapter ("всё, что эмитит OTel GenAI спаны интегрируется маппингом"), so the raw path is scoped as integration work, not magic; and the acceptance criteria are about tree-structure parity, not zero-integration. The word "per-tool" (не per-product) is the key: routing tools once through the SDK is not "per-tool code."

====================================================================================================
[LOW] PLAUSIBLE  sdk#2  (sdk)
FR: FR-SDK-4, NFR-SCALE-1, R5
TITLE: Tail-sampling '100% on errors and slow' is impossible in a thin in-process SDK and forces the Collector into the required path
CLAIM: FR-SDK-4 asks the SDK to do tail sampling (default ~5%, keep 100% of errors/slow) while §1/§5 sell a 'thin SDK'; the repo has BatchSpanProcessor with NO sampler (apps/server/src/tracing.ts), and tail decisions require seeing a whole trace after it ends.
WHY: A per-span head sampler (the only thing an in-process SDK can cheaply do) must decide at root-span START, before any error or latency is known — so it can keep a % of traces but cannot guarantee 100% of errors/slow. True tail sampling needs a stateful component that buffers all spans of a trace until completion; in OTel that is the Collector tailsamplingprocessor. Failure scenario: team ships 5% head sampling believing errors are fully captured; a production regression's failing traces are dropped at the same 5% rate, so the post-deploy error cluster (FR-FAIL-3) under-counts and the alert either misfires or never fires — the exact anti-pattern R6/R7 warns against, now caused by the sampling design itself.
FIX: State explicitly that '100% on errors/slow' requires the OTel Collector (tailsamplingprocessor) as a REQUIRED component, not optional — and reconcile that with the 'thin SDK' framing and self-hosted/VPC deploy (someone must run the collector). In-process, offer only head sampling + always-keep for spans the app can flag as errors at creation (explicit recordOutcome('fail') can drive a keep hint, but not latency).
VERIFY: The finding's OTel physics is correct (in-process head sampling can't guarantee 100% of errors/slow; true tail-sampling needs the Collector's tailsamplingprocessor) and the repo claim is accurate: apps/server/src/tracing.ts line 28 uses BatchSpanProcessor(new OTLPTraceExporter()) with no sampler. BUT its central claims are refuted by the PRD. C3 (line 215) fixes "OTel Collector" as a mandatory stack component, so the collector is NOT optional and cannot be "forced into the required path" — it is already required. FR-SDK-5 (line 96) already routes PII redaction through "redaction-процессор коллектора," i.e. the PRD already treats the collector as a pipeline processing stage. FR-INGEST-1/§9 (lines 104, 205) require OTel Collector compatibility. The finding's own recommendation ("state the Collector is REQUIRED, not optional; someone must run it") is thus largely pre-satisfied by C3. What survives is a genuine but minor traceability seam: FR-SDK-4 (line 95) is filed under §6.2 "SDK инструментации" and phrased as sampling *control* attributed to the SDK, while the mechanism that delivers "100% on errors/slow" actually lives in the Collector — the PRD nowhere explicitly says tail-sampling is a Collector concern, so a literal implementer could wrongly build it in the thin SDK (line 68). The catastrophic failure scenario (ship 5% head sampling, under-count post-deploy errors, break FR-FAIL-3/R6) only fires if an implementer ignores C3's mandated Collector, which the PRD forbids. That reduces this from a feasibility contradiction to a documentation/layer-attribution nit.
STEELMAN: Even mitigated, the PRD has a real seam worth a one-line fix: FR-SDK-4 lives in the SDK section and reads as an SDK capability ("настраиваемый tail-sampling"), but tail-sampling with a keep-100%-of-errors guarantee is architecturally a Collector (tailsamplingprocessor) responsibility. The PRD never draws that boundary explicitly — it only implies the Collector exists via C3 and FR-SDK-5. An engineer building "FR-SDK-4" against the current tracing.ts (which already has a BatchSpanProcessor and no sampler) could plausibly add a head Sampler in-process and believe FR-SDK-4 is done, silently under-sampling exactly the error/slow traces FR-FAIL-3's post-deploy alert depends on. Explicitly moving the "100% on errors/slow" clause to the ingestion/Collector layer (or adding a note under FR-SDK-4 that the SDK only provides head sampling + an always-keep hint via recordOutcome('fail'), and the Collector does tail sampling) would close the ambiguity at near-zero cost and harden the R5/R6 mitigations.

====================================================================================================
[LOW] PLAUSIBLE  eval#3  (eval)
FR: FR-EVAL-3, FR-EVAL-5, NFR-PRIV-1, FR-INGEST-5
TITLE: The 'weekly mine-from-prod → label → add case' loop cannot be fed: prod telemetry is content-free by design
CLAIM: FR-EVAL-3/FR-EVAL-5 require sampling prod failures and turning them into eval cases, but the existing prod audit trail (ask_telemetry) stores only a sha256 of the question and no answer/input/trace, so there is nothing to sample into a case.
WHY: ask-telemetry.ts documents 'only the sha256 of the normalised question is stored, never the question text'. A golden case needs the input + a reference/rubric; a hash is unrecoverable. The PRD then compounds this with NFR-PRIV-1 (redact PII BEFORE storage) and FR-SDK-4 (~5% tail sampling), so even the net-new trace store will hold redacted, sub-sampled payloads. Failure scenario: the weekly loop samples 5% of redacted traces where the reference answer was never captured; a human/agent labeler faces '[REDACTED] asked something, agent failed' with no ground truth to author against — the loop stalls and the golden set never grows, silently reverting to a static curated set (exactly today's build-eval-cases.ts FACTS[] array).
FIX: Specify the case-mining data contract explicitly: which content fields (input, output, tool I/O, reference) are captured at what fidelity for cases flagged failed, and reconcile it with the redaction/sampling policy (e.g. 100% content capture on outcome=fail per FR-SDK-4's '100% on errors', quarantined store, redaction that preserves labelability). Without this the FR-EVAL-3 loop is a spec with no data source.
VERIFY: The finding's central premise leans on the WRONG system. `ask_telemetry` (verified: /Users/duchenchuk/Desktop/AgenticMind/packages/shared/src/database/schema/knowledge/ask-telemetry.ts:16-18 — stores only question_hash sha256, never text) is the LEGACY /ask knowledge-service audit trail. APL does not source cases from it: the PRD builds a NET-NEW SDK + trace store (FR-CONTRACT, FR-SDK, FR-INGEST-3 ClickHouse). So "prod telemetry is content-free by design" is true only of a table APL doesn't use. On the net-new store the PRD DOES provide content capture: FR-CONTRACT-7 captures gen_ai.input.messages/output.messages/system_instructions (flag-controlled) and NFR-PRIV-1 mandates "управляемый content-capture" (managed content-capture) — not absence. The sampling starvation argument is also substantially wrong: FR-SDK-4 already mandates "100% на ошибках" (100% on errors), and FR-EVAL-3 mines FAILURES specifically — exactly the fully-captured class, so 5% tail-sampling does not starve the fail-mining path. The data model even names eval_case.source(curated|mined) with input + reference/rubric (line 190), so the mined-case output contract exists. What genuinely survives is narrower: the PRD never specifies the case-MINING data contract — which content fields at what fidelity for outcome=fail traces, and critically how the ground-truth reference/rubric is authored (prod traces contain input + the agent's FAILED output + outcome, never a correct reference) and whether NFR-PRIV-1 redaction preserves labelability. That gap is real but is ordinary eval-curation work (human authors the reference), not a "loop cannot be fed / no data source" blocker. The finding's recommendation (specify the mining data contract) is sound; its diagnosis is inflated. Repo claims verified accurate (FACTS[] array at build-eval-cases.ts:37; ask-telemetry comment verbatim), but they describe today's pre-APL state, not a PRD contradiction.
STEELMAN: The strongest defense of the PRD: it explicitly models the exact mechanism the finding claims is missing. eval_case.source has a `mined` value (line 190), FR-EVAL-3 (MUST) defines the weekly fail→label→add loop, FR-CONTRACT-7 provides managed content capture of input/output messages, FR-SDK-4 guarantees 100% capture on errors (the mining target), and NFR-PRIV-1 says content-capture is "managed" (governed, not zeroed). A golden case needs input + reference/rubric; the input and the failed output are captured at full fidelity for failures, and authoring the reference from a labeler is standard eval practice, not a data-availability blocker. The redaction-vs-labelability tension is a real implementation detail but is one the PRD's "управляемый content-capture" phrasing explicitly reserves for the design — it is not a contradiction in the requirements.

====================================================================================================
[LOW] PLAUSIBLE  judge#2  (judge)
FR: FR-JUDGE-4, FR-EVAL-2, FR-FAIL-2
TITLE: Sharing one human-annotation pool for calibration AND taxonomy is label leakage the repo explicitly forbids
CLAIM: FR-JUDGE-4 says human annotations (FR-EVAL-2c) are reused for both judge calibration and error taxonomy, but the taxonomy (FR-FAIL-2 axial coding) and the judge later score/act on overlapping cases, so the judge's measured TPR/TNR is inflated by evaluating on data entangled with its own tuning/label set.
WHY: If the same annotated failures seed the named clusters AND serve as the judge's calibration ground truth, then (1) the judge is scored against labels that also defined the categories it will be asked to detect, and (2) L2/L3 improvement proposals derived from those clusters are validated by a judge calibrated on the same examples — a closed loop that reports progress on its own training data. eval/README.md line 40-41 states the in-repo rule directly: 'keep the oracle distinct from the judge under test to avoid circularity.' Concretely: an L3 auto-merge (FR-IMPROVE-3) 'confirms a gain' because the patch was fit to the very cluster the judge was calibrated on; the regression it introduces on unseen inputs is never caught.
FIX: Split the annotation pool by purpose: a held-out calibration set that is never used to derive clusters or few-shot patches, and a separate taxonomy/mining set. Document a no-overlap invariant (assert trace_refs disjoint) enforced at build time, exactly as build-eval-cases.ts asserts unique ids. Downgrade FR-JUDGE-4 from 'reuse' to 'may share the annotation WORKFLOW but not the same examples across calibration and eval'.
VERIFY: The kernel is real: FR-JUDGE-4 (line 131) + data model line 195 (`annotation ... (калибровка + таксономия)`) genuinely route ONE human-annotation pool into both judge calibration and error taxonomy, and the PRD states no held-out/no-overlap invariant. But the finding's core mechanism is a misread. (1) The judge is NOT a cluster classifier — it answers a binary per-case quality/faithfulness question (FR-EVAL-2b, line 120); the taxonomy (FR-FAIL-2, line 137) is a separate downstream embedding-cluster artifact. So "scored against labels that also defined the categories it will be asked to detect" is false: the judge is never asked to detect the named clusters. (2) "Inflated TPR/TNR" imports a test-on-train intuition, but the judge is a versioned prompt (FR-JUDGE-1), not a model fit to the labels; reusing annotations for taxonomy does not change the measured agreement rate on the calibration set. (3) "Regression on unseen inputs never caught" ignores FR-IMPROVE-3 (line 147), which requires an INDEPENDENT calibrated judge to confirm gain on the per-agent golden set (curated+mined, FR-EVAL-1) AND no new clusters, then a mandatory A/B against prod before promote/rollback — live unseen traffic is the final gate. (4) The eval/README line 41 quote ("keep the oracle distinct from the judge under test") is about Level-3 oracle-vs-judge independence (stronger-model review), which the PRD honors via FR-IMPROVE-3's "независимый ... судья"; it is NOT a calibration-set-vs-taxonomy-set disjointness rule, so the quote is repurposed for a different circularity than it addresses. Repo reality reinforces the steelman: build-eval-cases.ts keeps judge-labels.json (calibration ground truth, hand-curated obvious-grounded/ungrounded pairs, README line 38) as a SEPARATE artifact from cases.json, each with its own dedup assert (lines 287, 342) — the calibration pool is already distinct in nature from mined failure traces. Net: a genuine documentation/invariant gap (add a no-overlap assert; cheap best-practice) but the "high-severity label leakage the repo forbids" framing overstates it via judge≠classifier and calibration≠training errors, and the A/B stage mitigates the closed-loop harm.
STEELMAN: Even accepting the mechanism errors, the finding's remediation is sound and cheap: an explicit held-out calibration set that never seeds clusters or few-shot patches is textbook hygiene, and adding a disjointness assertion at build time mirrors the repo's own unique-id discipline. There is a residual real risk: if an L2/L3 few-shot patch (FR-IMPROVE-2 "few-shot из хороших трейсов") is drawn from the very traces that also calibrated the judge and defined the target cluster, the pre-A/B eval-gate signal for that cluster carries an optimistic bias, so the A/B stage does more work than the design implies. Documenting the no-overlap invariant would make the Cycle-of-Trust guarantees explicit rather than incidental, and it costs almost nothing to add to FR-JUDGE-4 and the data model.

====================================================================================================
[LOW] unverified  sdk#7  (sdk)
FR: FR-SDK-3, FR-JUDGE-2
TITLE: Judge min-label-count (≥100) is not enforced by the reusable calibration analogue, so an under-calibrated judge can gate the SDK's outcome hooks
CLAIM: FR-SDK-3 outcome/feedback hooks feed the judge path; the existing, reusable calibration lib (packages/shared/src/lib/eval/judge-calibration.ts) enforces TPR/TNR>0.8 but NOT the ≥100-label minimum FR-JUDGE-2 requires — `total` is only reported, and computeCalibration returns tpr=1 when the positive set is empty.
WHY: A judge calibrated on, say, 12 labels (11 positive) can show TPR=1.0/TNR=1.0 and be marked calibrated=true, then be trusted to score SDK-captured outcomes. Failure scenario: outcome='fail' auto-triage (FR-FAIL-1) and L3 gating (FR-IMPROVE-3) run on a judge that passed the threshold on a tiny sample, so the whole self-improvement envelope rests on statistical noise (anti-pattern R2) — and the SDK's recordFeedback data that should fix this is exactly what was too small.
FIX: When adapting judge-calibration.ts for APL, add a hard min-N gate (calibrated = tpr>t && tnr>t && total>=100 && both class counts sufficient) and refuse the empty-class tpr=1 shortcut. Wire the SDK's human-feedback capture (FR-SDK-3) to accumulate toward that N.
