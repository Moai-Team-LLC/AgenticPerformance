/**
 * Vendored from AgenticMind: single source of truth for vector() column
 * dimensions. Keep in sync with the embedding model used to embed fail-traces
 * / cluster labels (bge-m3 = 1024).
 */
export const EMBEDDING_DIMENSIONS = 1024
